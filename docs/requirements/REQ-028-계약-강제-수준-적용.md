---
id: REQ-028
uid: GD0BBSZ8
type: document
kind: requirement
title: 계약 강제 수준 적용
description: evaluator violations에 advisory 경고 또는 checkpoint 저장·동기화 차단을 적용하는 요구사항
granularity: bounded-v1
scope: 문서 계획 계약 위반의 강제 수준 적용과 차단 경계
excludes:
  - 위반을 계산하는 evaluator 내부 규칙
  - 계약 revision 변경 절차
implementationContract: atomic-v1
functionIds:
  - FN-001
owner: "[[project#^MEMBER-001|프로젝트 책임자]]"
state: active
tags:
  - rundol/artifact
  - artifact/requirement
  - domain/rundol
  - feature/document-contract
aliases:
  - REQ-028
related:
  - "[[PRD-001-Rundol-제품-요구사항|PRD-001]]"
  - "[[REQ-010-문서-계획-계약-요구사항|REQ-010]]"
---

# 계약 강제 수준 적용

## 배경

강제 수준 적용은 [[REQ-010-문서-계획-계약-요구사항|REQ-010]]이 나르던 기능 하나를 문서 1개 = 기능 1개 계약에 따라 분리한 정본이다. 같은 위반이라도 팀의 단계에 따라 경고로 둘지 저장·동기화를 막을지가 다르므로, 강제 수준은 계약이 명시적으로 정하고 적용 경계는 push 전으로 고정한다.

## 요구사항

- advisory 위반은 경고로, checkpoint 위반은 strict/save/sync 차단 오류로 처리한다.
- recommended 누락은 강제 수준과 무관하게 경고다.
- checkpoint 위반은 저장과 동기화를 차단하되 초기 bootstrap과 일반 태스크 생성은 막지 않는다.

## 상태와 예외

| 현재 상태 또는 상황 | 사건 | 기대 상태 또는 동작 |
|---|---|---|
| checkpoint 위반 | save 또는 sync | push 전에 실패한다. |
| advisory 위반 | check 또는 save | 경고를 반환하고 작업은 계속한다. |

## 기능별 설계 계약

### FN-001

#### 입력

- enforcement 값과 evaluator violations

#### 출력

- advisory 경고 또는 checkpoint 차단 결과

#### 업무 규칙

- recommended 누락은 항상 경고이며 required와 disabled 위반만 checkpoint 저장·동기화를 차단한다.

#### 상태와 전이

- 위반이 해소되면 별도 승인 없이 다음 검사에서 차단 상태가 해제된다.

#### 권한과 승인

- enforcement 변경은 계약 revision 기반 명시적 변경으로만 허용한다.

#### 정상·오류·취소

- bootstrap과 일반 태스크 생성은 허용하고 save·sync는 push 전에 실패해 원격을 변경하지 않는다.

#### 감사 기록

- enforcement와 위반 진단 코드는 계약·명령 결과와 Git 이력에 남는다.

#### 수용 기준

- checkpoint 위반 상태의 save와 sync는 커밋·push 전에 실패하고 advisory는 경고로 계속한다.
