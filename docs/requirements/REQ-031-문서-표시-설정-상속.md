---
id: REQ-031
type: document
kind: requirement
title: 문서 표시 설정 상속
description: 문서 유형·상태의 표시 문구를 내장 기본값, Workspace, 프로젝트 board.json 순으로 상속하는 요구사항
granularity: bounded-v1
scope: 문서 유형과 상태 표시 설정의 상속 해석과 화면 일관성
excludes:
  - 문서 계약 정책과 강제 수준
  - 문서 ID, kind와 저장 경로의 의미
implementationContract: atomic-v1
functionIds:
  - BRD-03
owner: "[[project#^MEMBER-001|프로젝트 책임자]]"
state: active
tags:
  - rundol/artifact
  - artifact/requirement
  - domain/rundol
  - feature/document-planning-board
aliases:
  - REQ-031
related:
  - "[[PRD-001-Rundol-제품-요구사항|PRD-001]]"
  - "[[REQ-011-보드-문서-계획과-표시-설정-요구사항|REQ-011]]"
---

# 문서 표시 설정 상속

## 배경

표시 설정 상속은 [[REQ-011-보드-문서-계획과-표시-설정-요구사항|REQ-011]]이 나르던 기능 하나를 문서 1개 = 기능 1개 계약에 따라 분리한 정본이다. 문서 유형과 상태의 표시 문구가 화면마다 다르면 사용자는 같은 것을 다른 것으로 읽는다. 표시 설정은 문구·설명·정렬만 바꾸며 정본 의미는 바꾸지 않는다.

## 요구사항

- 문서 유형과 문서 상태의 표시 설정은 내장 기본값, Workspace `board.json`, 프로젝트 `board.json` 순으로 상속한다.
- 표시 설정은 문구, 설명과 정렬 순서만 바꾸며 문서 ID, kind, 저장 경로와 계약 의미를 바꾸지 않는다.
- 표시 설정 JSON은 schemaVersion 1을 사용하고 알 수 없는 필드는 무시하지 않고 진단한다.
- 사이드바, 최근 문서, 카드와 상세 화면이 동일한 유효 표시 문구를 사용한다.

## 상태와 예외

| 현재 상태 또는 상황 | 사건 | 기대 상태 또는 동작 |
|---|---|---|
| 설정 파일 없음 | 조회 | 상위 기본값을 사용한다. |
| 잘못된 JSON·필드 | 조회 | 파일 경로를 포함해 실패한다. |

## 기능별 설계 계약

### BRD-03

#### 입력

- 내장 기본값, Workspace board.json과 프로젝트 board.json

#### 출력

- 문서 유형·상태별 유효 label, description, order와 각 값의 출처

#### 업무 규칙

- 내장값보다 Workspace, Workspace보다 프로젝트 override를 우선하며 식별자·kind·경로는 바꾸지 않는다.

#### 상태와 전이

- 설정 파일 추가·수정·삭제는 다음 snapshot에서 즉시 유효 설정을 재계산한다.

#### 권한과 승인

- Workspace 설정은 workspace 브랜치, 프로젝트 설정은 해당 프로젝트 브랜치의 소유 경계를 따른다.

#### 정상·오류·취소

- 파일이 없으면 상위 기본값을 사용하고 잘못된 JSON·필드는 파일 경로를 포함해 실패한다.

#### 감사 기록

- 원본 JSON과 병합 출처가 snapshot 및 각 브랜치 Git 이력에 남는다.

#### 수용 기준

- 사이드바, 최근 문서, 카드와 상세 화면이 동일한 유효 표시 문구를 사용한다.
