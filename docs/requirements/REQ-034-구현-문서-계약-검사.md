---
id: REQ-034
uid: 58J4CW0B
type: document
kind: requirement
title: 구현 문서 계약 검사
description: atomic-v1 구현 문서의 기능 ID별 누락·미확정·묶음·중복·무원천을 공용 검사기가 판정하는 요구사항
granularity: bounded-v1
scope: 구현 문서 계약의 기계 검사와 진단의 단일 의미
excludes:
  - 문서 생성과 frontmatter 발급
  - 태스크 완료 전환 게이트
implementationContract: atomic-v1
functionIds:
  - FN-001
owner: "[[project#^MEMBER-001|프로젝트 책임자]]"
state: active
tags:
  - rundol/artifact
  - artifact/requirement
  - domain/rundol
  - feature/명령줄-ai-스킬-보드-통합-규격
aliases:
  - REQ-034
related:
  - "[[PRD-001-Rundol-제품-요구사항|PRD-001]]"
  - "[[REQ-012-명령줄-AI-스킬-보드-통합-규격|REQ-012]]"
  - "[[TST-002-문서-계획-계약-검증|TST-002]]"
---

# 구현 문서 계약 검사

## 배경

구현 문서 계약 검사는 [[REQ-012-명령줄-AI-스킬-보드-통합-규격|REQ-012]]이 나르던 기능 하나를 문서 1개 = 기능 1개 계약에 따라 분리한 정본이다. 문서 1개가 기능 1개를 나르는 것이 기본 계약이고, 검사는 그 계약을 표면과 무관하게 같은 의미로 판정하는 단일 검사기가 수행한다.

## 요구사항

- 범위 표기, 한 행의 복수 기능, 공유 placeholder·수용기준을 허용하지 않고 각 기능의 유형별 필드를 모두 검사한다.
- 일반 strict에서는 legacy 문서를 경고하고 implementation 모드에서는 준비되지 않은 계약을 오류로 승격한다.
- 공용 검사기가 판정하며 스킬과 Board는 진단의 심각도와 의미를 재정의하지 않는다.

## 상태와 예외

| 현재 상태 또는 상황 | 사건 | 기대 상태 또는 동작 |
|---|---|---|
| 완전한 독립 계약 | 검사 | 통과한다. |
| 외부 위임·미확정 값·누락 필드 | 검사 | 기능 ID를 대상으로 실패한다. |

## 기능별 설계 계약

### FN-001

#### 입력

- atomic-v1 구현 문서의 frontmatter와 기능별 본문

#### 출력

- 기능 ID별 누락·미확정·묶음·중복·무원천 진단

#### 업무 규칙

- 범위 표기, 한 행의 복수 기능, 공유 placeholder·수용기준을 허용하지 않고 각 기능의 유형별 필드를 모두 검사한다.

#### 상태와 전이

- 일반 strict에서는 legacy 문서를 경고하고 implementation 모드에서는 준비되지 않은 계약을 오류로 승격한다.

#### 권한과 승인

- 공용 검사기가 판정하며 스킬과 Board는 진단의 심각도와 의미를 재정의하지 않는다.

#### 정상·오류·취소

- 완전한 독립 계약은 통과하고 외부 문서로의 규칙 위임, 결정되지 않은 값과 누락 필드는 기능 ID를 대상으로 실패한다.

#### 감사 기록

- 진단은 code, file, artifactId, target과 line을 안정된 JSON으로 반환한다.

#### 수용 기준

- 선언으로 다기능이 허용된 문서(TST·MOD·API)의 여러 완전한 기능은 통과하고, `PAY-01~04` 같은 범위 표기나 한 통합 행은 RDL-IMPL-004 오류가 된다. REQ·SCR의 다기능은 선언 여부와 무관하게 RDL-IMPL-014로 거부한다.
