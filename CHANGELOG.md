# 변경 이력

이 문서는 사용자에게 영향을 주는 Rundol 변경을 기록한다. 버전 분류와 tag 규칙은 [버전과 릴리스 정책](docs/RELEASES.md)을 따른다.

## [Unreleased]

### 규칙이 실제로 발화한 이력을 남긴다

어떤 규칙이 실제로 일하고 있는지 아무도 몰랐다. 발화 이력이 없으면 "한 번도 안 걸린 규칙"과 "완벽해서 안 걸리는 규칙"이 같아 보인다. 둘은 정반대다 — 앞은 지우거나 고쳐야 할 것이고 뒤는 지금 일하고 있는 것인데, 겉모습이 같아 어느 쪽인지 물을 수조차 없었다.

어휘를 데이터로 여는 설계가 이것을 필수 부품으로 만들었다. 닫힌 어휘의 제약은 코드를 고쳐야 죽으므로 리뷰가 한 번은 본다. 열린 어휘의 제약은 설정을 잘못 고치는 것만으로 죽고, 그것을 보는 눈은 없다.

세는 축을 둘로 둔다. `evaluated`는 판정이 실제로 본 규칙이고 `blocked`는 그중 막은 규칙이다. 막은 것만 적으면 "한 번도 안 막은 규칙"과 "한 번도 안 불린 규칙"이 같은 침묵이 되는데, 그 둘은 정반대의 뜻이다. `rdl rule dead`가 이 둘을 `never`와 `silent`로 나누어 내는 이유가 그것이고, `rdl rule bypasses`는 면제를 사유·결정자와 함께 낸다 — 면제는 판정 자체를 돌지 않는 것이라 본 것으로 세지 않는다.

이력은 `firing` 원장에 쌓인다. 로컬 로그가 아닌 이유는 물음이 프로젝트 전체의 것이기 때문이다. 어휘가 이름을 먼저 정했고 event store의 등록부가 그것을 받는다. 같은 판정은 다시 적지 않으므로 원장은 저장소의 활동량이 아니라 서로 다른 판정의 수만큼 자란다.

적는 자리는 하나다. 판정부가 자기가 본 것을 값으로 돌려주고 검사가 그것을 모아 넘긴다. 판정 지점마다 심으면 지점이 늘 때 계측을 빠뜨리게 되고, 빠뜨렸다는 사실은 아무 신호도 내지 않는다 — 이 계측이 생긴 이유와 똑같은 실패다.

이력을 처음 켜자마자 두 가지가 드러났다. `implementation-readiness` 게이트는 유형 해석기 밖에 있어 한 번도 불리지 않은 죽은 규칙으로 집계됐고, 태스크가 든 완료 게이트 면제는 게이트 함수가 자기 안에서 걸러 이력에 아예 남지 않았다 — 우회가 "다들 지키는 규칙"으로 세어지고 있었다. 둘 다 이 변경에서 함께 고쳤다.

의미는 건드리지 않았다. 무엇이 위반인지, 어떤 전환이 허용되는지, 어떤 게이트가 면제될 수 있는지는 전부 그대로다. 기록에 실패해도 검사의 진단과 종료 코드는 같다 — 계측이 판정을 막으면 그 순간 계측은 규칙이 된다.

## [0.39.9] - 2026-08-23

### 한글 앞의 셸 변수를 중괄호로 묶는다

macOS의 `/bin/sh`는 bash 3.2이고, 비UTF-8 로케일에서 변수 뒤에 붙은 한글 바이트를 변수 이름으로 빨아들인다. `$default에`는 "default에"라는 없는 변수가 되고 `set -u`가 거기서 끝낸다.

그래서 `pre-commit` 훅은 기본 브랜치의 제품 코드 커밋을 막기는 했지만 왜 막혔는지는 말하지 못했다. 안내를 내는 그 줄에서 죽었기 때문이다. 막는 것보다 다음에 무엇을 할지 말하는 것이 이 훅이 하는 일의 절반인데, 그 절반이 macOS에서만 조용히 빠져 있었고 0.39.7 이후 배포 검사가 계속 빨간불이었다.

안내문은 거의 다 한글이므로 같은 실수가 다음 훅에서도 자연스럽게 다시 나온다. 실제 커밋으로는 리눅스와 윈도우에서 재현되지 않으므로, 행동이 아니라 설치된 바이트의 모양을 시험이 본다 — 어느 기계에서 돌든 같은 답이 나온다.

### 같은 밀리초에 든 할당 사건의 순서를 인과가 정한다

할당 원장은 클라이언트마다 조각을 따로 쓰고 읽을 때 합친다. 합칠 때의 순서는 기록 시각이 정했고, 시각이 같으면 `eventId`가 정했다. `eventId`는 무작위다.

그래서 발급과 보고가 같은 밀리초에 들어가면 순서가 동전 던지기가 됐다. 절반의 확률로 보고가 발급보다 앞서 접혔고, 앞선 보고는 가리킬 할당이 없어 `RDL-ASG-903`으로 버려졌으며, 그다음 검수는 "접수된 보고가 없습니다"로 멈췄다. 개발 기계에서는 두 사건이 같은 밀리초에 들지 않아 한 번도 보이지 않았고, 빠른 기계에서 도는 0.39.8 배포 검사에서 처음 모습을 드러냈다.

이제 시각이 같을 때는 인과가 가른다. 있어야 가리킬 수 있고, 접수되어야 판정할 수 있고, 판정되어야 닫힌다. 순서의 정본은 어휘가 들고 있으며, 사건 유형이 늘어날 때 순서를 빠뜨리면 어휘 시험이 막는다.

기계 사이의 시계가 밀리초 너머로 어긋나는 경우는 이 변경이 다루지 않는다. 그때는 순서가 아니라 기준 시각의 문제다.

## [0.39.8] - 2026-08-23

### 보호 경로에 스킬·배포 설정·시험을 넣는다

커밋 경계가 보는 경로가 `src/` `bin/` `packages/` `scripts/` 넷뿐이었다. 실행되는 코드만 제품 코드로 본 것인데, 그 밖에도 기본 브랜치에서 조용히 바뀌면 안 되는 것이 있다.

`skills/`는 `rdl skill install`이 세 AI 클라이언트의 홈에 복사한다. 한 줄을 고치면 그 저장소를 쓰는 모든 에이전트의 판단이 바뀌므로, 파급이 실행되는 코드보다 넓으면 넓었지 좁지 않다. `.github/`는 실려 나가지는 않지만 무엇이 언제 npm에 올라가는지를 정한다. `test/`는 통제가 실제로 서 있는지를 판정하는 층이며, 여기가 자유로우면 통제를 지우는 가장 조용한 길이 검사를 고치는 것이 된다.

`docs/`는 대상이 아니다. 이 저장소에서는 검사가 문서와 명령의 일치를 판정하지만 그것은 Rundol 자신의 결합이고, 이 목록은 Rundol을 쓰는 모든 프로젝트에 실려 나가는 기본값이다. 남의 저장소에서 문서 한 줄 고치는 데 브랜치를 요구하는 것은 이 도구가 정할 일이 아니다.


## [0.39.7] - 2026-08-23

### 훅이 꺼져도 서는 커밋 경계

`rdl git init`이 관리형 `pre-commit`과 `commit-msg`를 함께 설치한다. `pre-commit`은 기본 코드 브랜치에서 제품 코드를 담는 것을 거절하고, `commit-msg`는 제품 코드 커밋에 `Rundol-Task` 트레일러를 요구한다.

클라이언트 훅과 층이 다르다. `PreToolUse`와 `Stop`은 AI 클라이언트 안에서만 돌고 설정 한 줄로 꺼지므로, 그 통제에는 **사람 모양의 구멍**이 남는다. 사람 워커와 에이전트 워커가 같은 계층이라면 같은 경계를 지나야 한다.

두 훅으로 나눈 것은 필요한 값이 다르기 때문이다. 브랜치와 경로는 메시지 없이 판정할 수 있지만 결박은 메시지가 있어야 보인다. 한 훅에 몰면 막힌 이유가 브랜치인지 결박인지 사유 한 줄로 갈리지 않는다.

**안착 경로는 열려 있다.** git은 `git merge`에 `pre-commit`을 돌리지 않고, `commit-msg`는 `Merge`·`Revert`로 시작하는 메시지에 결박을 묻지 않는다. fast-forward와 병합 커밋 둘 다 지난다 — 막기만 하고 나갈 길이 없으면 경계가 아니라 벽이다. 기본 코드 브랜치는 `origin/HEAD`에서 읽고 없으면 판정하지 않으며, 기존 훅은 `<이름>.rundol-user`로 보존해 먼저 실행한다.

우회는 `--no-verify`로 열려 있고 막지 않는다. 손을 묶는 것이 아니라 기본값을 뒤집는 것이 목적이다 — 지금은 우회가 기본이고 규칙이 예외인데, 그 방향이 반대가 되면 규칙을 벗어나는 것이 매번 선택이 된다.

`rdl git boundary`가 두 훅의 설치 상태를 함께 낸다(`RDL-BRANCH-006`). 미는 자리만 보면 담는 자리가 열려 있어도 초록이 되기 때문이다. Git 훅은 추적되지 않으므로 clone마다 `rdl git init` 또는 `rdl attach`를 한 번 지나야 선다.

### 시험 워커 상한을 넷으로 낮춘다

코어 수만큼 워커를 띄우던 것을 넷으로 제한한다. 워커 하나가 `node`와 `git`을 쉴 새 없이 spawn하므로, 코어를 채우면 Windows가 프로세스 생성에서 무너진다 — 실제로 여덟 개로 돌렸을 때 `git init`이 `0xC0000142`로 끝났다. 그 실패는 시험의 실패처럼 보이지만 시험과 무관하고 다시 돌리면 사라져 진단할 수도 없다. 더 늘리려면 `RUNDOL_TEST_JOBS`로 올린다.


## [0.39.6] - 2026-08-23

### 시험이 6분 만에 끝난다

78개 파일을 한 프로세스에서 순차로 돌려 40분이 걸렸다. 시험 내용이 무거워서가 아니라 대부분이 `node bin/rdl.js`를 spawn하기 때문이다 — 한 번 띄울 때마다 런타임이 새로 오르고 모듈 수십 개를 다시 읽고 `git`을 또 여러 번 띄운다. 코어를 놀리면서 그 비용을 순서대로 치를 이유가 없다.

격리는 이미 있었다. 각 시험이 자기 임시 디렉터리를 쓰고 `RUNDOL_HOME`이 pid로 갈리므로 프로세스를 나누는 것만으로 서로를 보지 않는다. 새로 만든 것은 나누는 규칙뿐이며, 순서가 계약인 사슬은 한 워커에 그대로 둔다. `RUNDOL_TEST_JOBS=1`이면 예전처럼 한 프로세스에서 돈다 — 나눈 것이 원인인 실패를 되돌려 확인할 자리가 없으면 병렬은 진단할 수 없는 층이 된다.

**40분에서 6분 35초.** 목록을 만들면서 한 번도 실행된 적 없는 시험 파일 하나가 드러났고, 목록과 디스크가 갈리는 것을 앞으로 잡는 시험을 함께 뒀다.

### 붙어 있는 것을 다 본다

`rdl session list`가 `session/` 접두 브랜치만 걸러 보고 있었다. 이 명령이 답하는 물음은 "지금 이 저장소에 누가 붙어 있는가"인데, 자기가 만든 것만 세면 그 물음에 답하지 못한다. 실제로 손으로 만든 worktree 하나가 목록에 없는 채로 한 주를 지났고, 없다는 사실은 아무 신호도 내지 않았다.

이제 저장소의 모든 작업 트리를 낸다. 세션이 연 자리인지는 `managed`가 가른다 — 하나로 접으면 등록되지 않은 트리가 죽은 세션처럼 보이고, 그 목록으로는 회수할 것과 남의 작업을 구분할 수 없다. 본 작업 트리와 `rundol/*` 브랜치의 worktree는 뺀다. 그것은 붙어 있는 누군가가 아니라 저장소의 구조이고, 늘 있으므로 세면 답이 언제나 둘 이상이 되어 "혼자인가"를 물을 수 없게 된다.

### 제품 코드를 본 작업 트리에서 고치지 못한다

`rdl hook pre-tool-use`가 쓰기 전에 막는다. 되돌릴 것이 없는 유일한 시점이고, 커밋에서 막으면 작업은 이미 잘못된 자리에 쌓인 뒤다 — 옮기려면 `stash`와 `cherry-pick`이 필요하고 그 왕복 자체가 또 다른 사고 지점이다.

막는 조건은 셋이 모두 참일 때뿐이다. 본 작업 트리이고, 브랜치가 저장소의 기본 코드 브랜치이고, 대상이 제품 코드다. 하나라도 아니면 통과한다 — 문서를 고치는 것도, 세션 worktree에서 코드를 고치는 것도, 기능 브랜치에서 고치는 것도 막을 이유가 없다. 기본 브랜치는 `origin/HEAD`에서 읽고 없으면 판정하지 않는다. 거부 사유에는 다음 명령을 담는다 — 사유만 주면 부르는 쪽이 우회를 발명한다.


## [0.39.5] - 2026-08-23

### 닫히지 않는 태스크를 사람의 사유로 닫는다

수용조건을 다 채우고도 완료되지 않는 태스크가 이 저장소에 열두 건 있었다. 완료가 요구하는 TST 연결이 없기 때문이고, 편집기 태스크 여섯 건은 대응하는 검증 문서가 서른여섯 개 TST 어디에도 없었다. 그 상태로 두면 태스크는 밀린 일이 아니라 **닫지 못한 일**로 `doing`에 쌓이고, 쌓인 `doing`은 저장의 태스크 파생을 죽인다. 닫지 못하는 것 하나가 결박 전체를 요금으로 바꾼다.

```bash
rdl task set TASK-... --status done --exempt done-requires-test-link --reason "검증 문서가 아직 없다"
```

반려와 같은 모양인 것은 우연이 아니다. 둘 다 증거 없이 상태를 옮기는 일이고, 그래서 둘 다 사유와 결정자를 요구한다. 사유가 없으면 면제는 거부된다 — 없으면 면제가 완료 게이트를 우회하는 조용한 통로가 되기 때문이며, 반려에 대해 이미 내린 판단과 같다.

면제할 수 있는 게이트는 코드가 정한 목록 안이다(`implementation-readiness`, `done-requires-test-link`). 되돌릴 수 없는 관문은 그 목록에 없으며, 그것이 면제가 경계를 여는 수단이 되지 않게 하는 유일한 장치다. `--exempt`는 반복할 수 있다 — TST가 없다는 사실 하나가 게이트 둘을 함께 막으므로, 하나만 면제하면 나머지가 여전히 닫는다. 게이트는 여럿이고 사유는 하나다.

면제는 게이트·사유·결정자·시각으로 태스크에 남고, 완료가 아닌 상태로 되돌리면 자동으로 지워진다. `rdl check` 요약이 면제 건수를 낸다 — 면제된 게이트는 진단을 내지 않으므로, 요약에 없으면 얼마나 쓰였는지 아무 데서도 보이지 않고 세지 않는 우회는 예외가 아니라 그냥 다른 길이 된다.

완료 게이트 판정을 `check.js`가 다시 선언하고 있었다. 정본에 면제를 넣고도 막히길래 드러났다 — 규칙이 두 곳에 있으면 둘은 갈린다. 정본을 내보내고 두 번째 선언을 지웠다.


### 코드 작업 자리가 저장소 안으로 들어온다

`rdl session start`가 만드는 worktree의 기본 자리를 저장소의 형제 경로에서 `.rundol/worktrees/<id8>`로 옮겼다. 형제에 두던 이유는 추적 제외 규칙이 로컬 `.git/info/exclude`에만 있어 clone한 사람에게 오지 않았기 때문인데, 규칙이 제품 브랜치의 `.gitignore`로 옮겨 오면서 그 전제가 사라졌다. 흩어진 형제 디렉터리를 사람이 찾아다니지 않아도 된다.

자리를 옮기는 결정과 그 자리를 지키는 규칙은 함께 서야 한다. 그래서 `rdl hook session-start`가 본 작업 트리에서 `/projects/*/`와 `.rundol/` 두 규칙의 존재를 확인하고, 없으면 채우고 무엇을 채웠는지 말한다. 규칙보다 먼저 만들어진 저장소와 규칙을 지운 저장소가 있으므로 사람의 기억에 맡기지 않는다. 이미 있으면 파일을 건드리지 않고, 하나만 빠져 있으면 그것만 채운다.

`--path`로 자리를 지정하는 길은 그대로다.


## [0.39.4] - 2026-08-23

### 결박이 지켜지지 않는 절반을 지표가 보게 한다

`rdl check`의 태스크 결박 지표가 프로젝트 브랜치만 세고 있었다. 코드 브랜치는 창 밖이었고, 그래서 저장소의 절반에서 결박이 무너져도 검사는 초록으로 답했다. 이 저장소가 실제로 그 상태였다 — 프로젝트 브랜치는 최근 50건 중 미결박 0건인데, 코드 브랜치는 21건이었다.

이제 두 줄을 따로 낸다. 코드 브랜치의 미결박은 `RDL-TASK-041`로, 등록되지 않은 태스크를 가리키는 커밋은 `RDL-TASK-039`로 나온다. 한 줄로 합치지 않는 이유는 문이 있는 쪽의 높은 결박률이 문이 없는 쪽을 가려 지표가 실제보다 건강해 보이기 때문이다.

세기만 하고 막지는 않는다. 코드 커밋은 `rdl save`를 지나지 않으므로 여기에는 막을 자리가 없고, 막을 수 없는 자리에서 오류를 내면 검사 전체가 꺼진다.

Workspace 루트가 Git 최상위가 아니면 세지 않는다. 다른 저장소 안에 놓인 Workspace가 바깥 저장소의 커밋을 자기 결박으로 보고하고, 그 저장소의 태스크를 모르니 전부 끊긴 결박으로 세게 된다.

### 하네스가 완료 주장을 대신 확인한다

`rdl hook`이 생겼다. AI 클라이언트의 훅에서 부르는 표면이며 `session-start`, `post-tool-use`, `stop`, `session-end` 넷을 받는다. 판정은 이 명령 하나에 있고 클라이언트의 훅 설정은 부르기만 한다 — 판정을 훅 스크립트에 두면 클라이언트마다 답이 갈린다.

`stop`이 이 턴에 생긴 커밋 중 태스크 결박을 지나지 않은 것을 찾아 되돌린다. 완료 주장을 사람의 보고가 아니라 Git으로 재확인하는 자리이며, 어느 턴의 커밋인지는 `session-start`가 놓아 둔 커서와의 차이로 센다. 커서가 없으면 아무것도 세지 않는다 — 처음 켠 훅이 과거를 이 턴의 위반으로 보고하지 않게 하기 위해서다.

되돌림은 한 턴에 한 번이다. 두 번째 회차는 통과시키고 커서를 전진시킨다. 이것이 없으면 고치지 않는 쪽과 훅이 무한히 주고받는다.

미커밋 변경은 알리기만 한다. 진행 중인 작업 트리가 더러운 것은 정상이고, 그것까지 막으면 훅이 소음이 되어 곧 꺼진다. 프로젝트 브랜치에서는 아무것도 막지 않는다 — `rdl save`가 이미 결박을 강제하므로 두 번 막으면 어디서 막혔는지 흐려진다.

판정하지 못하면 통과시킨다. 저장소 밖이든, Git을 부르지 못했든, 입력을 읽지 못했든 마찬가지다. 훅이 판정을 지어내면 막지 말아야 할 것을 막고, 그렇게 한 번 겪은 훅은 꺼진다.

계획과 이벤트별 배치는 [하네스 훅](docs/HOOKS.md)에 있다. Rundol은 이 훅을 설치하지 않는다 — 스킬 설치가 `postinstall`에 붙지 않는 것과 같은 규율이며, 문서의 스니펫을 각 저장소가 직접 놓는다.

### worktree 경로를 제품 브랜치가 숨긴다

linked worktree를 숨기는 규칙이 로컬 `.git/info/exclude`에 있었다. 그 파일은 복제되지 않으므로 새로 clone한 사람은 `rdl attach`를 지나기 전까지 규칙을 갖지 못하고, 그 사이의 `git add -A` 한 번이 worktree 전체를 커밋한다.

이제 제품 브랜치의 `.gitignore`가 숨긴다. 규칙은 둘이다 — 프로젝트 문서와 태스크 정본 worktree(`/projects/*/`), 그리고 코드 작업 worktree와 로컬 실행 상태(`.rundol/`).


### 세지 못한 것과 혼자인 것을 가른다

`rdl run pending`은 같이 붙어 있는 세션이 둘 이상일 때만 말한다. 그런데 세션 읽기 실패가 빈 목록으로 접히고 있었다. 두 처리를 곱하면 세션을 읽지 못하는 저장소는 영영 "혼자"로 보인다 — 경고가 필요한 바로 그때 침묵하는 방향이다.

이제 못 셌다는 사실이 값으로 남는다. `--json`은 `sessionsUnreadable`에 사유를 싣고, 사람이 읽는 출력은 `동시세션 확인불가` 줄을 낸다. 세션을 읽지 못해도 런의 답은 그대로 낸다 — 세션은 런과 같은 물음의 다른 축이고, 한 축의 실패가 다른 축을 삼키면 안 된다.

침묵의 조건도 말로 적었다. 런이 없고, 세션도 하나 이하이고, 그 세션을 셀 수 있었을 때만 아무것도 출력하지 않는다. 조건이 조용히 바뀌어도 시험이 그것을 단언하지 않으면 우연히 통과하고 있는 것과 구별되지 않는다.

### 떠 있는 것 하나가 편집기를 죽이지 않는다

블록 손잡이를 잡아도 끌리지 않는 일이 있었다. 원인은 손잡이가 아니라 툴바였다. ProseMirror는 플러그인 뷰의 `update()`를 한자리에서 부르므로, 툴바가 던진 예외가 뒤에 있던 손잡이의 끌기 준비를 대신 죽였다. 문서는 이미 갱신된 뒤라 타이핑은 멀쩡해 보였고, 그래서 죽은 것은 한참 뒤에 다른 증상으로 나타났다.

떠 있는 것들 — 툴바, 블록 손잡이, 슬래시 메뉴, 링크 고르기, 표 조작 — 은 전부 화면 좌표를 읽고, 좌표는 노드가 아직 그려지지 않았거나 떼어졌을 때 던진다. 이제 각자의 `update()`를 감싸 던지면 그 팝업만 감춘다. 자리를 못 잡은 메뉴보다 못 쓰게 된 편집기가 나쁘다. 다만 조용히 삼키지는 않는다 — 무엇이 던졌는지 콘솔에 남긴다. 삼키면 그 자리가 다음 결함의 숨을 곳이 된다.

## [0.39.3] - 2026-08-21

### 댓글이 줄기를 갖는다

논의는 줄기를 갖는다. 지금까지 댓글은 평면이라 누가 무엇에 답한 것인지가 시각 순서에만 담겨 있었고, 다섯 개가 쌓이면 세 번째가 첫 번째에 답한 것인지 두 번째에 답한 것인지 아무도 답할 수 없었다. 그 순간 논의 기록은 발언 목록이 된다.

이제 댓글에 **답글**을 단다. 화면에서는 답글 단추로, 명령줄에서는 `rdl task comment … --reply-to <EVT-ID>`로 같은 줄기에 붙는다.

**깊이는 하나다.** 답글의 답글은 계단을 만들지 않고 같은 줄기의 뿌리에 붙는다 — 계단을 열어 두면 다섯 칸 아래의 말이 어느 줄기에 답한 것인지 따라갈 수 없다. 접는 자리는 저장이다. 읽는 쪽에서 접으면 명령줄과 보드가 같은 원장을 다르게 접는다.

**뿌리 댓글에는 `parentId` 키가 생기지 않는다.** null을 넣으면 지난 이벤트와 필드 구성이 달라지고, `canonicalDigest`는 필드 구성을 그대로 반영하므로 이미 쌓인 기록의 재계산이 어긋난다. 없는 것은 없는 채로 둔다.

붙일 곳은 두 가지로 막는다. 없는 댓글에는 붙지 않고, 다른 태스크의 댓글에도 붙지 않는다 — 한 논의가 두 태스크에 걸치면 어느 태스크의 기록인지 물을 수 없게 된다.

### 댓글을 읽고 쓰는 자리를 다시 짠다

누가 남겼는지가 목록에서 먼저 읽혀야 한다. 이름에서 뽑은 색의 **아바타**가 붙고 — 같은 사람은 늘 같은 색이다, 무작위면 새로고침마다 바뀌어 얼굴 역할을 하지 못한다 — 에이전트는 채우지 않은 점선 테두리로 사람과 갈린다. 시각은 "3시간 전"으로 읽고 마우스를 올리면 정확한 값이 나온다.

**입력칸은 평소에 한 줄이다.** 항상 펼쳐 두면 목록보다 입력칸이 커 보이고, 읽으러 온 사람에게 쓰라고 재촉하는 화면이 된다. 답글 단추는 마우스를 올렸을 때만 나타난다 — 항상 보이면 목록이 단추밭이 된다.

**입력은 문서 편집기를 그대로 쓴다.** 그림 붙여넣기, `[[문서 링크`, 서식과 표가 댓글에서도 된다. 댓글용 편집기를 따로 들이면 같은 그림이 자리마다 다른 규격으로 저장되고 마크다운 방언도 두 벌이 된다. 편집기 번들이 없으면 입력칸으로 떨어진다 — 빌드 실패가 논의를 막는 사고가 되면 안 된다.

**쓰는 중에는 화면을 갈아치우지 않는다.** 문서 편집이 이미 쓰던 규칙이다. 폴링 한 번이 편집기를 통째로 버리면 쓰던 글이 같이 사라진다.

### 옆에서 연 태스크가 낡지 않는다

목록에서 태스크를 눌러 옆 패널로 열면, 스냅샷을 다시 읽어도 그 패널만 예전 값을 들고 있었다. 방금 남긴 댓글이 저장되고도 그 자리에서는 보이지 않아, 사람은 댓글이 사라진 것으로 읽고 같은 글을 다시 썼다. 이제 목록과 함께 다시 그린다. 어느 줄을 열어 두었는지 표시도 함께 남는다.

### 사람 게이트를 보드에서 지난다

사람 게이트는 런을 멈추게 하려고 있는 것이고, 그것을 지나는 유일한 경로는 human 자격의 승인이다. 그런데 그 경로가 명령줄에만 있어서, 승인해야 하는 사람이 터미널을 열고 런 ID와 Client ID를 옮겨 적어야 했다. 멈춘 런은 아무도 보지 않으면 그대로 남으므로 승인의 지연이 절차의 지연이 된다.

보드에 **런** 화면이 생겼다. 사람을 기다리는 런, 이어서 몰 수 있는 런, 지금 구동 중인 런을 갈래대로 보여 주고, 사람 게이트는 그 자리에서 승인한다. 갈래와 사유는 `rdl run pending`이 쓰는 판정을 그대로 읽는다 — 화면이 다시 판정하면 같은 런에 명령줄과 다른 답을 내는 두 번째 판정자가 생긴다.

**승인자는 화면에서 고른 활성 human Client다.** 이 기기의 작성자 신원은 승인자가 될 수 없다. 그 값은 태스크 샤딩이 쓰는 기기 ID이고 유형이 human이 아니며, human으로 바꾸면 같은 기기의 실행 명령이 전부 막힌다. 승인용 자격은 실행용 자격과 따로 있어야 한다.

**승인 화면은 승인할 것을 담는다.** 런의 목표, 지나온 스텝, 그리고 대상 문서의 본문을 문서 화면과 같은 렌더 경로로 그린다 — 표가 표로, 다이어그램이 다이어그램으로 보여야 읽고 판단할 수 있다. 대상 문서는 런이 시작할 때 지목한 것과 스텝이 만들어 낸 것을 모두 싣는다. 저작 절차는 런 도중에 문서를 만들기 때문에, 시작 이벤트만 보면 정작 승인할 문서가 목록에서 빠진다.

**하네스가 띄운 보드는 승인하지 않는다.** human 자격을 하네스가 들 수 없다는 것이 사람 게이트의 전부이므로, 그 표면이 자격을 HTTP로 빌려주면 게이트는 이름만 남는다. 조회는 그대로 둔다 — 무엇이 막혀 있는지는 하네스도 알아야 사람에게 가져갈 수 있다.

구동·재개·정지·소유권 이전은 보드에 두지 않았다. 사람만 풀 수 있는 것과 기계가 이을 수 있는 것은 다른 일이고, 한 화면에 섞으면 승인이 "막힌 것을 미는 단추"로 읽힌다.

### 등록은 하려던 일 위에서 끝난다

지난 판에서 등록 폼이 설정 화면에 생겼다. 그런데 미등록이 드러나는 자리는 설정 화면이 아니다 — 문서 편집을 누른 순간, 댓글을 쓴 뒤 보내기를 누른 순간, 동기화를 실행한 순간이다. 그 자리에서 화면이 하던 말은 여전히 "이 명령을 실행하세요"였고, 사람은 하던 일을 접고 터미널을 찾은 뒤 돌아와 무엇을 하려던 참이었는지부터 다시 세워야 했다.

이제 그 자리에서 등록을 받는다. 편집 단추를 누른 미등록 기기에는 명령줄 대신 등록 절차가 열리고, **등록을 마치면 누르려던 편집이 이어서 열린다.** 댓글은 쓰던 내용을 그대로 다시 보내므로 두 번 쓰지 않는다. 동기화도 같은 자리에서 이어진다 — 다만 push 확인은 다시 묻는다. 되돌리기 어려운 동작의 확인을 등록이 대신 눌러 줄 이유는 없다.

**등록 칸은 화면 전체에 한 벌만 있다.** 설정 화면은 같은 절차로 들어가는 문일 뿐 자기 칸을 갖지 않는다. 같은 이름의 입력이 화면에 둘이면 무엇이 저장될지는 사람이 채운 칸이 아니라 먼저 그려진 칸이 정한다.

**등록을 닫으면 하려던 일도 함께 버린다.** 남겨두면 한참 뒤의 등록이 예전에 누르던 것을 되살린다.

미등록 거절에는 종류가 붙었다. 화면이 거절 문장을 뒤져 원인을 되짚으면 말을 다듬는 순간 판정이 깨지므로, 서버가 `unknown-client`를 함께 돌려준다. 안내 문구는 화면 경로를 먼저 가리키고 명령줄은 뒤에 남긴다 — 화면 없이 쓰는 경로가 있기 때문이다.

## [0.39.2] - 2026-08-21 (게시되지 않음)

태그를 밀 때 `docs/CLI.md`의 명령 요약이 `rdl --help`와 어긋나 release check가 멈췄고, 게시 단계에 이르지 못했다. 태그는 되돌리지 않으므로 같은 내용을 `0.39.3`으로 냈다. 그 판의 변경은 위 `0.39.3` 항목이 갖는다.

## [0.39.1] - 2026-08-21

### 미등록 기기를 화면에서 등록한다

전에는 "명령줄로 등록하세요"라고만 알렸다. 그런데 미등록이 드러나는 자리는 대개 무언가를 하려던 순간 — 댓글을 남기거나 저장하려던 때 — 이고, 그때 사람을 터미널로 보내면 하던 일이 끊긴다. 등록은 이 기기의 신원을 적는 일이지 위험한 일이 아니다.

설정 → Clients에서 이 기기가 등록되지 않았으면 등록 폼이 나온다. 서버의 등록 API는 이미 있었고 화면만 붙지 않았던 자리다.

**식별자는 고를 수 없다.** 이 기기의 값은 프로젝트가 이미 알고 있고, 사람이 고르게 두면 다른 기기의 것을 적어 두 기기가 한 신원을 공유할 수 있다. 그러면 누가 남긴 기록인지 물을 수 없다.

**유형에 기본값을 두지 않는다.** `device`는 기계의 종류일 뿐 행위 주체를 담지 않아서 그 값으로 파생한 판정이 실제로 틀린 적이 있다. 사람이 쓰는 기기면 사람을, AI가 쓰면 에이전트를 고르게 한다.

삭제는 여전히 명령줄이 갖는다. 지운 Client의 기록은 남는데 그 신원을 화면에서 지울 수 있으면 무엇이 남긴 기록인지 물을 수 없게 된다.

## [0.39.0] - 2026-08-21

### 설정 자유도를 가역성으로 나눈다

설정을 어디까지 열 것인가에 답하는 기준이 생겼다. 조직 위계가 아니라 **행위의 가역성**이다 — AI에게는 직급도 신뢰 이력도 없어 위계 기반 권한을 붙일 자리가 없지만, 되돌릴 수 있는지 없는지는 사람과 AI 모두에게 같은 답을 낸다.

설정이 세 층으로 나뉜다. **경계**는 되돌릴 수 없는 관문이라 조이기만 가능하고 설정 화면에 아예 나오지 않는다. **정책**은 되돌릴 수 있으므로 자유롭게 풀 수 있으나 변경이 계약 변경 결정으로 남는다. **내용**은 판정에 쓰이지 않으므로 완전히 자유롭다.

경계 층 이름이 `board.json` 어디에 나와도 읽는 시점에 거부한다. 화면에 두지 않는 것만으로는 파일로 우회하는 길이 남고, 무시하면 적은 사람은 적용됐다고 믿는다.

### 승인 모드 네 가지

사람만·AI 혼합·AI 우선·AI만을 `board.json`의 `approval`에 적는다. 모드는 "AI를 얼마나 믿느냐"의 눈금이 아니라 **사람의 주의를 어디에 쓸지의 배분표**다 — 게이트가 촘촘하면 사람은 읽지 않고 누르기 시작하고, 그 순간 통제도 함께 사라진다.

네 모드는 새 기제가 아니라 사람 게이트·검증자 정책·승인 근거·위임 요구 넷의 조합에 붙인 이름이다. 이름 하나가 조합 전체를 정하며 일부만 바꿀 수 없다.

**AI만이 승인 없음을 뜻하지 않는다.** 승인 이벤트는 승인자와 행위자를 구성원 식별자로 요구하므로, 사람이 미리 위임한 명의로 AI가 승인하고 그 위임은 만료·취소된다. 책임 주체는 어느 모드에서도 사람이다.

작업공간이 바닥을 깔면 프로젝트는 그보다 조일 수만 있다. **바닥은 벽이 아니라 바닥이다** — 스텝이 바닥보다 낮은 값을 선언하고 있으면 거부하지 않고 끌어올린다. 거부하면 바닥을 까는 순간 기존 절차가 열리지 않고, 그러면 사람들은 바닥을 꺼 버린다.

### 검증 정책의 완화를 단조성 검증이 잡는다

계층 상속은 조일 수만 있다는 것이 규칙인데 검증 정책만 그 밖에 있었다. 판정 함수가 `step.verify[key]`를 보았고 실제 값은 `step.verify.policy` 안에 살았으므로, 부모 값을 읽지 못한 판정이 조용히 건너뛰어졌다.

실측하면 여섯 가지 완화가 전부 통과했다. 검증자를 셋에서 하나로, 정족수를 셋에서 하나로, 허용 반박과 기권을 영에서 다섯으로, 어댑터 다양성 요구를 끄기, 정책 객체를 통째로 지우기.

조임 방향은 필드마다 다르다. 검증자와 정족수는 커질수록 조이고 허용 반박과 기권은 작아질수록 조인다. **방향이 선언되지 않은 필드는 통과시키지 않고 거부한다** — 조용히 빠지면 새 손잡이가 생길 때마다 그것만 단조성 밖에 남는다.

### 업무 유형을 설정에서 정의한다

유형 하나를 늘리는 데 코드 열 곳 이상을 고쳐야 했다. 이제 `board.json`의 `itemTypes`에 적으면 코드를 고치지 않고 새 유형이 판정을 받는다.

규칙은 코드가 가진 제약 다섯 종류(`fields`·`requiresLink`·`requiredWhen`·`unique`·`exempt`)에 값을 채우는 방식이다. 조건식과 계산식은 받지 않는다 — 받는 순간 규칙 언어가 되고, 규칙을 읽으려면 규칙 언어를 먼저 읽어야 한다.

면제 가능 게이트는 코드가 가진 **허용 목록**이며 경계 층 게이트는 거기 없다. 유형 추가가 경계 우회 수단이 되면 안 되기 때문이다.

**호환성 파괴**: 태스크 종류 진단 코드가 `RDL-TASK-026`~`032`에서 `RDL-ITEM-001`~`006`으로 바뀐다. 잡히는 태스크와 개수는 같으며 이름만 달라진다.

### 기계 종류를 사람으로 올려세우지 않는다

`device` 유형 Client로 남긴 댓글이 사람이 쓴 것으로 기록되고 승인 근거 자격까지 얻었다. Client 유형은 기계의 종류이고 작성 주체는 그것을 쓰는 쪽이라, 같은 데스크톱을 사람도 AI도 쓰는 조합에서 반드시 틀린다.

이제 `device`는 에이전트로 떨어진다. 사람이 근거 자격을 원하면 `human` 유형 Client를 등록한다.

잘못 파생된 기록을 바로잡는 정정을 더했다. 원본을 고치지 않고 덧붙이며, 내리는 방향으로만 가고 사유를 요구한다.

### 설정 화면에 표시 규칙·승인·업무 유형이 보인다

표시 그룹 일곱이 모두 화면에 나온다. 전에는 문서 유형 하나만 그렸고 나머지 여섯은 파일을 열어야 보였다.

항목마다 값이 어느 계층에서 왔는지가 세 칸 막대로 보인다. 채움 개수가 정본이고 색은 보조라, 색을 지워도 읽힌다.

**경계 층은 목록에 없다.** 대신 무엇이 왜 없는지만 읽기 전용으로 적는다 — 잠긴 항목은 언젠가 잠금을 푸는 요청을 부르지만, 없는 항목은 그 대상이 되지 않는다.


### 사람이 자리에 없는 동안 런이 스스로 전진한다

`rdl run drive --scheduled`는 데몬을 띄우지 않는다. 그 플래그가 하는 일은 이 클라이언트가 설정된 스케줄러인지 확인하는 것뿐이며, **바깥의 스케줄러를 전제하고 만든 이음매**였다. 그 바깥이 비어 있는 동안 런은 사람이 명령을 칠 때만 전진했다. `rdl run driver`가 그 바깥을 상주 프로세스 하나로 채운다.

원장은 이미 사람 없이도 정확했다. 추가 전용이고 읽을 때마다 다시 접히므로 프로세스가 어디에서 끊겨도 커서가 복원된다. 없던 것은 상태가 아니라 그 상태를 보고 다음 한 걸음을 밟을 주체였다.

**설정하지 않은 Workspace에서는 한 런도 몰지 않는다.** 무인 실행의 동의는 하네스 설정의 `drive.schedulerClientId`이며 기본값이 `null`이다. 새 설정 키를 하나도 만들지 않았다 — 이미 있는 키가 이미 그 뜻이었다.

**판정을 새로 쓰지 않는다.** 무엇을 밀 수 있는지는 `rdl run pending`의 `drivable`이 이미 답하고 있었고, 드라이버는 그 답에 조건을 하나도 더하지 않는다. 더하고 싶어지는 넷 — 소유자가 나인가, 절차가 멱등인가, 설정이 표류하지 않았는가, 어댑터가 켜져 있는가 — 은 전부 실행 직전 검사가 이미 판정하며, 옮겨 적으면 같은 물음의 판정자가 둘이 된다.

### 전진하지 않은 런을 다시 밀지 않는다

`run drive`가 돌아왔는데 **원장에 아무것도 남지 않는 경로가 여덟** 있다. 그런 런은 판정상 여전히 몰 수 있는 것으로 보이므로 다음 회전에 다시 뽑히고, 같은 자리에서 같은 일이 일어난다.

대표가 `termination-unsafe`인데 이것은 정지 사유 목록에 없어 **기록되려 해도 거부된다.** 그리고 Windows에서 `RUNDOL_ALLOW_WINDOWS_ADAPTER`를 켜지 않으면 그것이 예외가 아니라 **모든 런의 첫 어댑터 스텝이 만나는 기본 경로다.** 격리 장치 없이 드라이버를 내보냈다면 첫날 밤에 같은 런을 수만 번 밀었을 것이다.

그래서 진행 여부를 반환값이 아니라 원장을 다시 읽어 판정한다. fold 필드 넷 — 상태, 커서, 완료 집합, 시도 횟수 — 이 그대로면 격리하고, 격리된 런은 증거가 달라질 때까지 고르지 않는다. 사람이 `rdl run resume`을 치거나 설정을 고치면 원장이 움직이고 **격리가 스스로 풀린다.** 백오프도 재시도 상한도 시계도 쓰지 않는다.

격리는 프로세스 기억이며 저장하지 않는다. 저장하면 새 저장 형식이 생기고 그 형식은 원장이 이미 말하는 것을 두 번 말한다.

### 소유권을 옮기지 않는다

드라이버는 세션과 같은 Client 식별자를 써도 된다. 소유권이 바뀌지 않으므로 이양 규약이 필요 없고, 프로세스 배타는 잠금이, 실행 안전은 operation 식별자의 멱등성이 이미 보증한다.

`rdl run takeover`는 런을 **기계 사이로** 옮기는 장치다. 자동 인수는 이전 소유자가 눈에 보이게 정지했을 것을 요구하며, 드라이버가 일상적으로 강제 인수를 하면 그 기록이 나르던 신호가 파괴된다.

### 에이전트 클라이언트가 런과 할당을 알게 된다

거버넌스 스킬에 `rdl run`이 **한 번도** 나오지 않았다. 사람이 마주하는 표면이 AI 클라이언트인데 그 클라이언트가 런의 존재를 몰랐으므로, 원장도 절차도 게이트도 아무도 쓰지 않는 기능이었다.

스킬이 이제 조회·시작·재개·승인 경로를 밝히고, 세션이 쓸 실행 단위를 정한다. **그 단위는 취향이 아니라 절차가 정한다** — `rdl run drive`는 멱등으로 pin된 절차만 받으므로 저작 절차를 아예 실행할 수 없다. 저작은 `next`/`step`으로 걷고 검증은 `drive`로 완주한다. 저작이 멱등이 아닌 이유도 함께 적었다: 무엇을 쓸 것인가는 사람이 정하는 의도이고 절차가 지어낼 것이 아니다.

소유권에 대해서도 적었다. 자기 기계의 일을 이어가는 데는 교대가 필요 없고, 강제 인수는 기계가 정말 사라졌을 때만 쓴다 — 일상적인 이유로 기록된 강제 인수는 그 기록이 나르던 신호를 파괴한다.

### 새 명령

- `rdl run driver --client-id <id> [--project <key>] [--interval <초>] [--once]` (고급 명령)
### 시작할 때 누가 같이 있는지 안다

같은 저장소에서 세션 여럿이 부딪힌 사고는 전부 사후에 발견됐다. 시험이 멈춰서, 구문 오류가 나서, 자기 작업이 이미 커밋돼 있어서 알았다. 공통점은 시작할 때 아무도 몰랐다는 것이다.

`rdl session start`가 세션을 등록하고 `rdl run pending`이 그 목록을 함께 낸다. 둘 이상일 때만 `동시세션` 줄이 나오며 각자의 작업 공간 경로가 붙는다 — 혼자일 때도 무언가를 뱉는 표면은 곧 읽히지 않는다.

등록은 새 저장 형식을 만들지 않는다. 런타임 잠금 디렉터리가 이미 "누가 무엇을 쥐고 있는가"를 pid와 함께 적는 자리이고, 세션 등록이 묻는 것도 같은 물음이다.

생존은 호스트가 알려주는 pid가 답한다. `RUNDOL_SESSION_PID`, `CLAUDE_PID` 순으로 받으며 `rdl` 자신의 pid는 쓰지 않는다 — 명령마다 죽으므로 그 값으로는 "모른다"를 "죽었다"로 바꿔 적게 된다. 그래서 `alive`는 셋을 구분한다. 붙어 있음, 등록했다가 끝남, 등록을 지나지 않음.

조회는 읽기만 한다. 등록을 겸하면 무엇이 주의를 요구하는지 묻는 행위가 상태를 바꾼다. 그리고 Rundol 작업공간이 없어도 세션은 답한다 — 세션 worktree에는 `projects/`가 없으므로 거기서 접으면 정작 격리된 세션이 물을 수 없다.

## [0.38.1] - 2026-08-21

### 일을 워커에게 내보내고 결과를 받는 경로가 열린다

판정부는 `0.37.0`에 들어왔지만 부를 사람이 없었다. `acceptReport`와 `verifyReport`가 답할 준비를 마친 채로 아무도 묻지 않는 상태였고, 그 사실을 그때 CHANGELOG가 "판정부는 아직 실제 접수 경로에 연결되지 않았다"고 적어 두었다. 이제 연결한다.

`rdl assignment issue`가 할당을 발급하고, 워커가 `rdl assignment report`로 보고하고, `rdl assignment verify`가 검수한다. 통과하면 할당이 닫힌다 — 통과한 일을 열어 두면 그 경로가 계속 배제되어 다음 할당이 막힌다.

**발급 거부 다섯과 접수 거부 일곱이 서로 구분된다.** 뭉개면 "발급이 안 된다"만 남고 무엇을 고쳐야 하는지가 사라진다. 그리고 접수 거부와 검수 반려는 다른 사건이다 — 앞엣것은 보고를 잘못 만든 것이고 뒤엣것은 일을 통과시키지 못한 것이다. 같은 값으로 뭉개면 워커 종류별 형식 위반율을 잴 수 없고, 그 수치는 PRD-001의 편익 지표 하나다.

**같은 경로를 두 워커에게 주지 않는다.** 발급 시점에 열린 할당의 수정 가능 경로와 겹치는지 보고, 겹치면 어느 할당의 어느 패턴과 겹쳤는지까지 돌려준다. 하나만 알려주면 통제자가 어디까지 좁혀야 통과하는지 모른 채 왕복한다.

**워커 신원은 주장이 아니라 파생이다.** 보고가 스스로 "나는 사람"이라고 적을 수 있으면 AI가 쓴 것을 AI가 읽고 승인 근거로 삼는 길이 열린다. 등록된 Client 유형에서 파생하며, 그 표는 태스크 댓글이 쓰는 것과 같은 하나다.

### 거부는 남되 상태를 만들지 않는다

거부된 발급도 원장에 사유와 입력 요약을 남긴다. 거부가 침묵하면 무엇을 시도했는지가 사라진다. 그러나 거부 사건에는 할당 식별자가 없고 원장을 접을 때 상태 계산에서 빠진다 — 기록은 남고 할당은 생기지 않으므로 부분 발급이 남지 않는다.

### 저장하지 않고 계산하는 것 셋

추가 전용 원장에서는 지난 사건을 고칠 수 없다. 그래서 대체 표시, 절차 일치 여부, 태스크 상태를 저장하지 않고 읽을 때마다 계산한다.

보고가 여러 번 올라오면 마지막을 뺀 전부가 다음 보고의 식별자를 가리킨다. 대체됐다는 사실과 기록이 사라지지 않는다는 요구가 동시에 지켜지고, 아무것도 다시 쓰지 않는다.

절차 일치는 할당의 고정 다이제스트와 보고가 밝힌 다이제스트의 비교다. 둘 다 불변이므로 저장하면 입력은 틀릴 수 없는데 저장된 값만 틀릴 수 있는 상태가 생긴다.

태스크 상태도 마찬가지다. 그리고 **태스크가 반려돼도 할당은 닫히지 않는다.** 자동으로 닫으면 할당 종료의 판정자가 셋이 되고 그중 하나는 할당을 쳐다보지도 않는 명령이 된다. 상태는 보여 주되 판정은 사람이 한다.

### 새 명령

- `rdl assignment issue <절차이름> --project <key> --client-id <id> --goal <목표> --acceptance <조건>... --function-id <기능-ID>... --allow-path <패턴>... --report-schema <이름> --procedure-revision <n> (--assignee-member <MEMBER-ID> | --assignee-client <client-id>)` (고급 명령)
- `rdl assignment list --project <key> [--open]` (고급 명령)
- `rdl assignment show <ASG-ID> --project <key>` (고급 명령)
- `rdl assignment cancel <ASG-ID> --project <key> --client-id <id> --reason <사유>` (고급 명령)
- `rdl assignment report <ASG-ID> --project <key> --client-id <id> --outcome <done|blocked|rejected> --report-schema <이름> --procedure-digest <digest>` (고급 명령)
- `rdl assignment reports <ASG-ID> --project <key>` (고급 명령)
- `rdl assignment verify <ASG-ID> --project <key> --client-id <id>` (고급 명령)
- `rdl session start [--session-id <id>] [--path <경로>] [--from <ref>]`
- `rdl session list`
- `rdl session end [--session-id <id>] [--force]`

### 같은 프로젝트를 두 저장이 동시에 쓰지 않는다

프로젝트 worktree는 프로젝트마다 하나뿐이다. 두 저장이 겹치면 뒤엣것의 `git add -A`가 앞엣것이 아직 커밋하지 못한 변경까지 담는다. 세션을 worktree로 나눠도 이 자리는 갈리지 않는다 — 코드와 달리 문서와 태스크는 공유하기로 한 것이고, 공유하기로 한 자리는 직렬화해야 한다.

`rdl save`가 프로젝트 단위 잠금을 잡는다. 같은 기계 안이라 원자적 생성과 프로세스 생존 확인으로 실제 배타가 되며, 그 기제는 이미 있던 것이라 새로 검증할 저장 형식이 없다.

기다리지 않고 거절한다. `RDL-SAVE-012`로 끝나며 누가 쥐고 있는지 pid를 함께 말한다. 저장은 세션이 지금 하려는 일이므로 붙잡아 두면 그 세션이 통째로 멈춘다. 프로젝트가 다르면 서로 막지 않는다.

### 세션마다 자기 작업 공간을 연다

한 기계에서 AI 세션 여럿이 같은 저장소를 고치면 서로의 변경이 조용히 섞인다. 경로를 나눠도 막히지 않는다 — `git add <내 파일>`은 그 파일에 들어 있는 남의 줄까지 함께 스테이지하고, 한 작업 트리의 index는 하나이기 때문이다. 그래서 "누가 무엇을 고치는가"를 아무리 잘 나눠도 커밋 혼입은 남는다.

`rdl session start`는 세션마다 worktree와 `session/<id8>` 브랜치를 하나씩 연다. index와 HEAD가 세션마다 갈리므로 남의 미커밋 변경이 내 커밋에 딸려 오지 않고, 시험이 도는 중에 소스가 바뀌지 않으며, 반쯤 쓰인 파일을 다른 세션이 실행하지 않는다.

세션 식별자는 발급하지 않고 받는다. `--session-id`, `RUNDOL_SESSION_ID`, `CLAUDE_CODE_SESSION_ID` 순으로 보고 없으면 만든다. 호스트마다 식별자를 두는 자리가 다르므로 중립 변수를 앞에 둔다 — 새 클라이언트는 어댑터가 그 변수를 채우면 Rundol을 고치지 않고 붙는다.

브랜치에 세션 식별자를 박는 이유는 추적이다. 커밋이 어느 세션의 일이었는지는 주석이 아니라 브랜치가 답해야 한다. 주석은 파일에 누적되고 같은 줄에서 충돌하지만 브랜치는 이력에 한 번 남고 병합과 함께 사라진다.

`--force`를 쓰지 않는다. 기본 거부가 이 명령이 지키려는 바로 그 불변식이다 — 한 브랜치는 한 작업 트리에만 있어야 하고, 두 곳에 걸리면 한쪽의 커밋이 다른 쪽 HEAD를 조용히 옮긴다.

코드 작업 공간만 나눈다. 문서와 태스크는 `projects/<key>` worktree 하나를 공유하므로 `rdl save`와 `rdl task`는 세션을 나눠도 같은 자리에서 직렬로 돈다.

## [0.38.0] - 2026-08-21

세 가지가 함께 들어온다. 정본 문서에 그림을 넣을 수 있게 되고, 태스크에 댓글이 붙고, 무엇이 개입을 기다리는지 한 번에 물을 수 있게 된다. 셋 다 "없으면 그 기능이 아예 없는 것과 같았던" 자리를 채운다.

새 명령이 다섯이라 minor를 올린다. 없어지는 명령은 없고 저장 파일도 바뀌지 않으므로 옮겨가며 할 일이 없다.

### 무엇이 개입을 기다리는지 한 번에 묻는다

런은 세션보다 오래 산다. 원장이 append-only이고 fold가 읽을 때마다 다시 계산되므로 프로세스가 어디서 죽어도 커서는 복원된다. 그런데 **그 사실을 물어볼 자리가 없었다.** `rdl run list`는 프로젝트 하나를 강제하고 fold 전체를 돌려주므로, "지금 내가 뭘 해야 하나"를 묻는 데 쓸 수 없다.

`rdl run pending`은 그 물음만 답한다. `--project` 없이 Workspace 전체를 훑고, 런을 사람만 풀 수 있는 것과 기계가 이어 밀 수 있는 것으로 가른다. 항목마다 그대로 붙여 실행할 수 있는 복구 명령이 따라온다.

세 번째 갈래를 둔 이유는 두 갈래로는 틀린 말을 하기 때문이다. 지금 드라이버가 몰고 있는 런을 `drivable`로 내면 이미 돌고 있는 것을 다시 몰라고 말하게 된다. 유효한 driver lease나 살아 있는 drive 프로세스 잠금이 있으면 `driving`이며, 이 갈래는 `--json`에만 나온다.

주의를 요구하는 런이 없으면 아무것도 출력하지 않는다. 이 명령은 세션이 열릴 때마다 도는 자리에 쓰려고 만들었고, 매번 무언가를 뱉는 표면은 곧 읽히지 않는다. 읽히지 않는 표면은 없는 것과 같다.

읽기만 한다. `reconcile`을 부르지 않고 잠금도 잡지 않는다 — 무엇이 주의를 요구하는지 묻는 행위가 원장을 바꾸면, 보는 것과 고치는 것이 같은 일이 된다.

### 자산 embed가 문서 참조로 오인되지 않는다

Obsidian은 문서 참조를 `[[REQ-001]]`로, 자산 끼워넣기를 `![[diagram.png]]`로 쓴다. 뒤엣것 안에 앞엣것의 문법이 그대로 들어 있어, 참조 검사가 둘을 가르지 않고 embed를 해결되지 않은 문서 참조로 판정하고 있었다. strict에서는 오류였으므로 정본 문서에 그림을 넣으면 저장과 동기화가 막혔다.

이제 `!` 접두와 자산 확장자가 함께 있으면 자산 embed로 판정하고, 실재하는 자산을 가리키는지를 본다. 끊긴 그림은 문서를 열어야만 보이므로 계산으로 잡아야 한다.

코드 스팬과 울타리 블록 안의 위키링크도 더 이상 참조로 보지 않는다. Obsidian도 코드 안의 링크를 걸지 않으며, 덮지 않으면 링크 문법을 설명하는 문서가 자기 예시 때문에 검사에 걸린다.

### `rdl asset add`가 큰 그림을 자동으로 줄인다

4000×3000 화면 갈무리를 사람이 매번 줄여서 넣으라고 하면 아무도 그 기능을 안 쓴다. 들여오는 순간 긴 변이 한계(기본 1600px)를 넘으면 비율을 지켜 줄인다.

PNG는 Node 내장 `zlib`으로 직접 다룬다 — PNG가 deflate로 압축된 필터링 스캔라인이라 내장 모듈로 끝난다. JPEG는 `jpeg-js`로 다룬다. 76KB에 자체 의존성이 0이고 순수 JavaScript라, 네이티브 바이너리도 설치 스크립트도 늘지 않는다. `sharp`는 958KB에 플랫폼별 바이너리를 설치 스크립트로 받아오므로 네 OS 매트릭스와 릴리즈 검사를 동시에 깬다.

SVG는 벡터라 줄이지 않는다. GIF와 WebP는 한계를 넘으면 거절하고 왜 못 줄이는지를 알린다 — LZW와 VP8은 zlib이 닿지 않고, 줄이지 못하는 것을 줄인 척하면 그 파일이 그대로 저장소에 남는다.

한계 이하인 파일은 원본 바이트를 그대로 쓴다. 다시 인코딩하면 손대지 않아도 될 파일이 매번 다른 바이트가 되어 diff가 무의미해진다.

이미 정본에 들어간 자산은 뒤에서 바꾸지 않는다. 이 명령이 손대는 것은 저장소 밖에서 가져오는 파일뿐이며, 들여오는 순간이 사람이 그 그림을 정하는 순간이다.

### 자산이 저장소를 조용히 뒤집지 못하게 한다

이 저장소는 문서 93개 전체가 780KB다. 화면 갈무리 한 장이 300KB면 그 하나가 문서 전체의 40%이므로, 방치하면 문서 브랜치는 곧 그림 브랜치가 된다.

`rdl check`가 자산의 용량(`RDL-ASSET-002`), 차원(`RDL-ASSET-003`), 알아보지 못한 형식(`RDL-ASSET-004`), 어느 문서도 참조하지 않는 자산(`RDL-ASSET-005`)을 본다. 넷 다 경고다 — 오류로 막으면 이미 쌓인 저장소가 판올림만으로 멈춘다.

차원은 헤더 앞부분 4KB만 읽어 잰다. 디코딩하지 않으므로 5MB 그림이 100장 있어도 읽는 것은 400KB다. 디코딩이 필요한 것은 변환이지 판정이 아니다.

자산은 문서와 같은 브랜치에 둔다. 브랜치를 나누면 문서와 그림이 다른 커밋에 있게 되어 "이 판의 그림"이 없어지고, 검사가 다른 브랜치의 파일을 판정할 수 없어 끊긴 참조를 잡지 못한다. clone 크기가 걱정이면 `--filter=blob:none`이나 sparse-checkout으로 받는 쪽에서 정한다. Git LFS는 쓰지 않는다 — 체크아웃에 네트워크가 필요해 로컬 우선 운영과 폐쇄망 사용을 동시에 깬다.

### prerelease 배포 경로가 실제로 동작한다

[버전과 릴리스 정책](docs/RELEASES.md)은 "검증 전 배포는 `0.18.0-rc.1`처럼 prerelease를 사용한다"고 적어 두었지만, 그 경로는 한 번도 밟힌 적이 없었고 두 군데가 막고 있었다.

`npm publish`의 기본 dist-tag는 버전이 prerelease여도 `latest`다. 릴리스 워크플로에 `--tag`가 없었으므로, `0.38.0-rc.1`을 그대로 올렸다면 `npm install -g rundol`을 하는 모든 사용자가 검증 전 판을 받았을 것이다. 이제 버전이 dist-tag를 정한다 — 하이픈이 있으면 `next`, 없으면 `latest`다. rc를 원하는 설치자는 `rundol@next`로 명시한다.

그리고 시험 하나가 버전 형식을 `^\d+\.\d+\.\d+$`로 손수 다시 정의해 prerelease를 거부하고 있었다. 형식의 정본은 릴리스 검사의 `SEMVER_PATTERN`이며 그것은 prerelease를 허용한다. 같은 물음에 판정자가 둘이면 언젠가 어긋나고, 실제로 어긋난 쪽이 문서가 약속한 것을 막았다.

### 새 명령

- `rdl asset add <파일경로> [--project <key>] [--as <이름>] [--max-edge <px>] [--doc <ARTIFACT-ID>]`
- `rdl asset list [--project <key>]`
- `rdl task comment <TASK-ID> <내용> --client-id <id> [--member <MEMBER-ID>] [--project <key>]`
- `rdl task comments [TASK-ID] [--project <key>]`
- `rdl run pending [--project <key>]` (고급 명령)

## [0.37.0] - 2026-08-20

이 판은 진단 출력의 필드 이름 셋과 워커 계약의 값 형태를 바꾼다. 없어지는 명령은 없고 저장 파일도 바뀌지 않는다. 옮겨가는 순서는 [Rundol 0.37 마이그레이션](docs/MIGRATION-0.37.md)에 있다.

외부 검토가 낸 결함 여섯을 닫는다. 하나는 릴리즈를 실제로 깨뜨렸고, 둘은 문서가 코드에 없는 것을 약속하고 있었고, 셋은 계측과 검사가 주장하는 것을 실제로 하지 않고 있었다.

### 릴리즈 게이트가 잠금 파일 오염과 타입 오류를 태그 전에 잡는다

`0.36.0`이 게시되지 못한 이유는 판올림이 잠금 파일에서 `packages/` 접두만 보고 워크스페이스를 골라 `packages/cli/node_modules/marked`까지 릴리즈 버전으로 덮었기 때문이다. 그 오염은 당시의 모든 로컬 검사를 통과했고 태그를 단 뒤 릴리즈 워크플로의 `npm ci`에서 처음 드러났다. 태그는 옮기지 않으므로 그 판은 버려졌다.

`version:check`가 이제 잠금 파일 무결성을 함께 본다. 내부 의존이 선언과 잠금에서 갈리지 않는지, 그리고 서드파티 항목의 잠금 버전이 그 항목이 가리키는 tarball과 같은지다. 대조 상대를 설치본이 아니라 `resolved` URL로 둔 이유는 오염된 바로 그 항목이 로컬에 설치돼 있지 않을 수 있기 때문이며, 대조하지 못한 항목을 조용히 건너뛰는 검사는 필요할 때 침묵한다.

`release:check`와 CI에 `typecheck`를 넣었다. 타입 선언을 계약 경계로 도입해 놓고 릴리즈가 그 계약을 검사하지 않으면, 선언은 주석과 같다.

### 폐기한 문서 소프트 임대가 문서에서도 사라진다

`rdl lease`와 보드의 임대 API는 `0.36`에서 폐기했지만 `docs/CLI.md`와 `docs/BOARD-API.md`는 여전히 정상 기능으로 설명하고 있었다. 마이그레이션 문서와 일반 문서가 서로 반대를 말했고, 문서대로 실행한 사용자는 폐기 오류를 받았다.

두 문서에서 임대를 걷어내고 그 자리에 폐기 사실과 대체 판정을 적었다. "고급 명령은 삭제가 아니라 은닉"이라는 설명도 임대에는 적용되지 않으므로 예외를 명시했다 — 임대는 내린 것이 아니라 지운 것이다.

### 같은 호출 안의 중복 링크가 한 번만 남는다

`rdl task set --link`가 신규 입력을 기존 목록과만 대조해서, 링크가 없던 태스크에 같은 값을 두 번 주면 둘 다 들어갔다. 걸러 낼 기존 목록이 비어 있었기 때문이다. 문서는 중복되지 않는다고 약속했고 시험은 기존 링크 재추가만 검사했다.

### 로그 상한이 UTF-8 바이트를 센다

상한 판정이 문자열 `length`를 썼는데 그것은 UTF-16 코드 단위이지 바이트가 아니다. 한글이 섞인 로그에서는 실제 크기를 3분의 1로 세어, 2MB 상한을 걸어 둔 채로 4MB가 남았다. 시험의 채움 문자도 ASCII뿐이라 구현이 주장하는 것을 증명하지 못했다.

`Buffer.byteLength`로 바꾸고 시험을 ASCII와 한글 두 경우로 돌린다.

### 개입 계측의 이름이 실제 측정 대상과 같아진다

`humanTurns`는 사람의 개입 횟수가 아니라 사람이 실행한 행위의 수를 셌다. 한 번 마음먹고 명령을 셋 치면 개입은 한 번이고 행위는 셋이므로 두 값은 같지 않다. 재고 싶은 이름을 붙여 두면 그 차이가 지워진 채로 편익 판단에 들어간다.

`humanActions`와 `medianHumanActions`로 바꾸고, 이 값이 명령을 거치지 않은 개입을 보지 못하는 **하한**이라는 사실을 `measures`와 `lowerBound`로 값에 실어 내보낸다. 주석에만 적으면 JSON을 읽는 쪽은 보지 못한다.

`rdl debug summary`의 출력 필드 이름이 바뀐다. 이 값을 읽는 자동화가 있다면 함께 고쳐야 한다.

### 보고 접수 계약이 선언대로 구현된다

`types/report.d.ts`가 선언한 접수 거부 사유 대부분에 대응하는 판정이 없었다. 이제 `acceptReport(할당, 보고)`가 필수 항목 누락, 다른 할당을 향한 보고, 닫힌 할당, 수임자가 아닌 보고자, 스키마 불일치, 차단·반려의 사유 누락, 언급하지 않은 수용 조건을 모두 판정한다.

접수와 검수를 나눈 이유는 둘이 다른 질문이기 때문이다. 접수는 "이 보고를 이 할당에 대해 판정할 수 있는가"를 묻고 검수는 "판정해 보니 통과인가"를 묻는다. 뭉개면 보고를 잘못 만든 것과 일을 통과시키지 못한 것이 같은 값이 되고, 그러면 워커 종류별 형식 위반율을 잴 수 없다 — 그 수치는 PRD-001의 편익 지표 하나다.

`Report`에 `schema`를 더했다. 보고가 따른 스키마를 밝히지 않으면 할당의 `reportSchema`는 아무도 읽지 않는 필수 항목이 된다.

판정부는 아직 실제 접수 경로에 연결되지 않았다. 할당 발급과 보고 저장은 명세부터 필요한 작업이며 배선 한 줄이 아니므로 별도 태스크로 남긴다. 호출자를 만들려고 판정을 제자리가 아닌 곳에서 부르는 것은 연결하지 않는 것보다 나쁘다.

## [0.36.2] - 2026-08-20

`0.36.0`과 `0.36.1`은 게시되지 않았다. 앞의 것은 판올림이 잠금 파일의 서드파티 버전까지 덮어 `npm ci`가 거부했고, 뒤의 것은 이번 판에 새로 들어온 두 시험이 개발자 기계에 붙은 작업공간에 기대는 바람에 릴리즈 검사에서 죽었다. 두 시험은 이제 임시 작업공간을 스스로 만들어 잰다.

내용은 아래 `0.36.0` 항목과 같다.

## [0.36.1] - 2026-08-20 (게시되지 않음)

`0.36.0`은 게시되지 않았다. 판올림 스크립트가 잠금 파일에서 `packages/`로 시작하는 항목을 전부 워크스페이스 패키지로 보고 버전을 덮었고, 그 접두는 `packages/cli/node_modules/marked` 같은 중첩 의존까지 잡는다. 릴리즈 워크플로의 `npm ci`가 잠금과 선언의 불일치로 거부해 게시 전에 멈췄다.

잠금을 다시 만들어 되돌렸고, 워크스페이스 루트만 고르도록 판올림 규칙을 좁혔다. 내용은 아래 `0.36.0` 항목과 같다.

## [0.36.0] - 2026-08-20 (게시되지 않음)

판정이 파일을 모르게 하고, 보장하지 못하던 임대를 걷어내고, 재려고 만든 계측을 실제로 켠다.

이 판은 호환성을 깬다. 없어진 명령과 엔드포인트, 그리고 조용히 건너뛰던 게이트가 깨어나는 것을 [Rundol 0.36 마이그레이션](docs/MIGRATION-0.36.md)에 정리했다.

### 문서 편집 소프트 리스 폐기 — 호환성 파괴

만료 시각에 기대는 배타는 공통 시계와 즉시 관측 가능한 공유 상태, 그리고 만료를 판정하는 단일 권위 셋이 있어야 성립한다. 중앙 서버 없는 Git 기반 구조에는 셋 다 없다. 그래서 이 기능이 준 것은 보장이 아니라 조언이었는데, 명령과 화면은 잠금처럼 보였다.

실사용 이벤트도 한 건에 그쳤다. 그동안 잠금 자체를 관리하다 생긴 결함이 이어졌고, 그 종류의 결함은 잠금이 없으면 존재하지 않는다.

- `rdl lease`가 폐기됐다. 부르면 근거 문서와 대체 수단을 알리고 멈춘다 — 조용히 사라지면 사람은 오타를 의심하지 손 뗄 근거를 찾지 못한다.
- 보드의 임대 엔드포인트 둘과 스냅샷의 `leases` 영역, 문서 저장 응답의 `leaseNotice`가 사라졌다.
- 이미 쌓인 임대 이벤트는 지우지 않는다. 지난 기록을 고쳐 쓰지 않는 것이 원칙이고, 폐기했다고 그때 그 일이 없던 것이 되지는 않는다.

대체는 이미 있었다. 저장 시점의 revision 비교가 낡은 쓰기를 `409`와 최신 내용으로 거절한다. 근거는 `ADR-015`에 있다.

### 사람 표면과 고급 표면 — 호환성 파괴

사람이 매일 쓰는 명령과 내부 실행 개념이 한 목록에 있으면 사람은 제품이 아니라 구현을 배워야 한다. 이름이나 인수에 실행 식별자·임대·클라이언트 식별자·어댑터·렌즈가 드러나는 열 개 명령군을 `rdl advanced` 뒤로 내렸다.

- `rdl --help`는 사람 표면만 낸다. `rdl advanced`가 나머지를 낸다.
- **삭제가 아니라 은닉이다.** 내린 명령은 그대로 동작한다.
- `rdl help --json`은 둘을 합쳐 돌려준다. 사람에게 숨기는 것과 에이전트에게 숨기는 것은 다른 판단이며, 여기서 숨기면 에이전트가 명령을 찾으려고 다시 소스를 뒤진다.

### `rdl task set --link` / `--unlink`

문서 링크는 태스크를 만들 때만 정할 수 있었다. 완료 게이트가 검증 문서 연결을 요구하는데(`RDL-TASK-019`) 링크를 나중에 붙일 방법이 없어, 링크 없이 만든 태스크는 명령줄로 영원히 닫히지 않았다. 가리키던 문서를 폐기해도 그 참조를 지울 수 없었다.

- 한 호출에 여러 번 쓸 수 있고 함께 쓸 수도 있다. 제거가 먼저 적용되므로 같은 호출로 교체가 된다.
- 태스크에 없는 링크를 빼려 하면 거절한다 — 조용히 넘기면 오타로 지운 줄 알고 넘어가고 정작 끊긴 참조는 그대로 남는다.

### 구현 준비도를 저장하지 않고 계산한다 — 호환성 파괴

`implementationReadiness`는 링크에서 결정되는 값인데 태스크를 만들 때 한 번 저장되고 그 뒤 갱신되지 않았다. 그래서 생성 이후 링크를 붙인 태스크는 준비도 게이트가 아예 돌지 않았고, 요구 문서 없이도 완료될 수 있었다.

- 필드를 저장하지 않는다. 남은 값은 읽지 않고 보존한다.
- 게이트는 연결된 문서가 실제로 원자 계약을 선언했을 때만 돈다. 그 계약을 쓰지 않는 프로젝트는 이전과 똑같이 동작한다.
- 진단 `RDL-IMPL-023`이 사라졌다. 판정할 저장 필드가 없다.

**조용히 건너뛰던 위반이 드러날 수 있다.** 마이그레이션 문서에 순서를 적었다.

### 준비도 진단이 붙일 문서를 알려준다

무엇이 없는지만 말하면 사람은 무엇을 붙여야 하는지를 다시 조사해야 한다. 실측에서 태스크 하나를 닫는 데 든 왕복의 상당수가 그 조사였다.

- `RDL-IMPL-020`·`021`·`022`가 어느 요구·시험 문서가 그 기능을 덮는지 함께 낸다.
- 모르는 기능에는 안내를 붙이지 않는다. 없는 문서를 지어내면 헛된 조사를 만든다.

### 계측을 기본으로 켠다

재려고 만든 계측이 `--debug`일 때만 돌아서 평상시 사용은 집계되지 않았다.

- 태스크 생성·수용조건·상태 변경에 더해 문서 작성, 검사, 저장, 동기화가 기록된다. 검사를 빼면 개입 횟수가 실제보다 좋게 나온다.
- 문서 작성은 저장이 쓰던 결박 사다리로 태스크를 추론한다. 저장은 자기가 정한 결박을 그대로 싣는다 — 추론을 두 번 하면 두 답이 갈릴 수 있다.
- 감시 모드는 세지 않는다. 한 번 걸어 두면 스스로 도는 것이라 세면 개입 횟수가 흐른 시간이 된다.
- 로그 상한은 2MB다. 넘치면 오래된 줄부터 버리되 방금 쓴 줄은 남긴다. 자르는 판정은 줄 수가 아니라 바이트다 — 줄 길이가 고르지 않다.
- 기록은 아무것도 막지 않는다. 프로젝트를 못 정하거나 로그를 못 써도 명령은 그대로 끝난다.

`rdl debug summary`가 태스크별 사람 개입 횟수와 중앙값, 그리고 결박되지 않은 행위 수를 낸다. 결박되지 않은 수를 함께 내는 이유는 그것을 감추면 중앙값이 전체를 대표하는 것처럼 읽히기 때문이다.

### 판정을 읽기부에서 떼어낸다

같은 저장소 상태에서 명령줄과 보드와 워커 어댑터가 다른 판정을 내면 사람과 에이전트는 같은 계층이 아니게 된다. 판정이 파일 읽기와 붙어 있는 한 각 표면은 자기 경로로 다시 구현하게 되고, 다시 구현한 것들은 조금씩 달라진다.

`check.js` 1,188줄에서 297줄을 값만 보는 판정 계층으로 옮겼다. 참조·링크·거버넌스, 문서 메타데이터, 태스크 규칙 스물아홉, 헌장과 계약 위반이 그 대상이다.

- 옮긴 모듈은 파일에 닿는 `require`를 갖지 않는다. 그 사실을 말로 지키지 않고 전이 의존까지 따라가는 시험으로 지킨다.
- 시험도 함께 바꿨다. 소스를 문자열로 뒤져 규칙이 있는지 보던 자리를 판정을 직접 불러 실제로 그렇게 답하는지 보는 것으로 옮겼다.

**남은 부분은 떼어내지 않았다.** 원장 무결성과 문서 수집과 설정 읽기는 호출자가 하나뿐이라 네 표면이 합의해야 할 판정이 아니다. 판정을 떼어내는 것은 둘 이상의 표면이 같은 답을 내야 할 때다.

### 진단이 자기 규칙의 정본 문서를 가리킨다

지금까지 진단은 코드와 메시지만 냈다. 어떤 규칙이 왜 존재하는지 알려면 검사기 소스를 뒤져야 했고, 반대로 요구를 고칠 때 어떤 검사가 흔들리는지도 알 수 없었다 — 추적은 계산 결과라면서 규칙에서 근거로 가는 링크가 비어 있었다.

- 진단 224개 중 124개가 정본 문서와 기능 식별자를 함께 낸다.
- 계열 전체를 한 덩어리로 붙이지 않았다. 태스크는 생성·전환·결박 셋, 결정은 요청·응답과 권한 경계 둘로 나뉜다.
- 나머지 100개는 그 규칙을 선언한 요구 문서가 없어 비어 있다. 추측으로 붙이면 근거가 아니라 지어낸 것이 된다.

### 그 밖

- 샤드 파일명 접두 겹침(`a` + `b-c` 와 `a-b` + `c`)을 시험으로 고정했다. 해소는 구분자 변경이 아니라 이름을 정하는 시점에 막는 것으로 굳혔다 — 구분자를 바꾸면 이미 쌓인 샤드 이름이 달라진다.
- 실행 원장의 작성자 시퀀스 번호를 채택하지 않기로 했다(`ADR-019`). 접기 결정성이 지금 깨지지 않고 실행 이벤트가 0건이라, 바꾸면 그 근거가 실사용이 아니라 상상이 된다.
- 문서와 태스크를 두 축으로 유지하기로 했다(`ADR-016`). 산출물과 가이드를 가르는 차이는 저장 위치가 아니라 참조 방식이었다.
- 타입 검사를 도입했다(`npm run typecheck`). `noEmit`이라 빌드 산출물이 생기지 않고 배포 구조도 바뀌지 않는다.

## [0.35.0] - 2026-08-19

결박이 답을 내기 시작하고, 외부 검토가 낸 안전성 결함 일곱을 닫는다.

### `rdl task commits`

`0.33`이 커밋에 `Rundol-Task` 트레일러를 적기 시작했지만, 그것을 읽어 답하는 경로가 없었다. `rdl check`가 세기만 했으므로 "이 태스크가 만든 커밋이 무엇인가"를 물을 방법이 없었다 — 적어 두기만 하고 아무도 읽지 않는 값은 메타데이터가 아니라 짐이다.

- `rdl task commits <TASK-ID>`가 그 태스크의 커밋을 낸다. `rdl task commits`는 전체 대응과 함께 우회·미결박 커밋도 낸다 — 묶인 것만 세면 무엇이 빠졌는지 알 수 없다.
- 작업 브랜치를 지워도 답이 남는다. 저장이 만든 커밋은 프로젝트 ref에 남기 때문이다. `--branch <name>`으로 다른 ref를 볼 수 있다.
- 없는 태스크를 물으면 빈 목록이 아니라 그 사실을 말한다. 없는 것을 "커밋이 없다"로 답하면 오타가 정상 응답이 된다.
- 창을 채웠으면 `truncated`로 알린다. 잘린 목록을 전부인 것처럼 돌려주면 못 본 것과 없는 것이 같은 값이 된다.

트레일러를 읽는 규칙을 한 곳으로 모았다(`src/task-commits.js`). 검사와 조회가 각자 파싱하면 두 곳이 같은 커밋을 다르게 읽는 날이 오고, 결박은 무엇이 사실인지를 다투지 않아야 값을 갖는 통제다.

### 안전성 — 외부 검토가 낸 일곱

- **커밋 없는 저장이 공유 장벽을 지나갔다.** 커밋을 만드는 스텝이 성공했다면서 커밋을 말하지 않으면 `producedCommits`가 비어 장벽이 그 런을 아예 보지 못했다. 장벽을 지나가는 가장 싼 길이 커밋을 적지 않는 것이었다. 완료를 취소하지는 않는다 — 커서를 되돌리면 지나간 런이 다시 돈다.
- **비활성 프로젝트 멤버도 run·verify를 실행할 수 있었다.** 문서에 이름이 남아 있는지만 보고 상태를 묻지 않았다. sync는 이미 상태를 보고 있었으므로 같은 물음에 두 경로가 다른 답을 내고 있었다.
- **어댑터를 여럿 고정한 검증을 재개할 수 없었다.** slot마다 어댑터가 달라 command가 다른데 재개가 동일성을 요구했다 — 재개 불가는 다양성을 켜지 않을 이유가 된다.
- **저작 병렬의 정상 실행을 sandbox 이탈로 오인했다.** 형제 경로를 지금 더러운 상태로 계산해서, 아직 복귀하지 않은 형제가 기준에서 빠지고 그 형제가 복귀하는 순간 탈출로 고발했다. `0.33.2`의 수정이 절반이었다.
- **`previousIds`가 조회에 쓰이지 않았다.** 식별자를 이관한 사람에게 자기 이력 전체가 끊긴 결박으로 보였다. 이관은 태스크를 옮긴 것이지 과거를 지운 것이 아니다.
- **Board 동기화 단추가 실패했다.** `clientId` 없이 `syncState`를 불렀다. 이제 이 기기의 Client를 쓰고, 등록되지 않았으면 스키마 오류가 아니라 무엇을 해야 하는지를 말한다.
- **명시한 런을 읽지 못하면 다른 태스크에 결박했다.** 답하지 못하는 근거와 읽지 못한 근거는 다르다. 앞은 넘어갈 일이고 뒤는 멈출 일이다(`RDL-TASK-040`).

### 샤드 파일명 접두 모호성

프로젝트 키 `a` + Client `b-c`와 `a-b` + `c`가 **완전히 같은 샤드 파일명**을 만들어 두 짝의 이벤트가 한 파일에 섞였다. 읽을 때 clientId 대조가 시끄럽게 실패하지만 그때는 이미 섞인 뒤다.

파일을 보고는 구분할 수 없으므로 — 이름이 같기 때문이다 — 이름을 정하는 자리에서 막는다. `rdl client register`가 겹치는 ID를 거부하고, 이미 겹친 저장소는 `rdl check`가 `RDL-EVENT-010`으로 드러낸다. 파일명 문법은 그대로 둔다. 바꾸면 이미 있는 샤드를 읽던 구버전이 멈춘다.

### 알려진 한계

- Windows에서 어댑터 자동 실행은 여전히 기본 차단이다(`RUNDOL_ALLOW_WINDOWS_ADAPTER`). Job Object로 프로세스 트리를 소유하는 계약과 세 OS 실측이 아직 없다.
- `implementationReadiness`는 여전히 저장된다. 없애면 그 필드가 없는 옛 태스크에 원자 계약 검사가 새로 걸려 진단이 넓어진다. 이관 경로를 함께 내는 별도 변경으로 다룬다.


## [0.34.1] - 2026-08-19

문서 유형 이관이 절반만 옮기고 있었다.

### 고친 것

- **`rdl doc migrate`가 태스크의 문서 링크를 옮기지 않았다.** `docs/`와 `inbox/`의 마크다운만 고쳐서, 이관이 끝난 뒤에 태스크가 없는 문서를 가리켰다. 실제로 이 저장소에서 `API-002`·`API-003`을 가리키는 태스크 둘이 그렇게 남았고, 이관을 돌린 사람에게는 `RDL-TASK-008` 오류로 보였다. 이름을 바꾸는 변경은 그 이름을 쓰는 모든 자리를 함께 옮겨야 이관이다 — 절반만 옮기는 것은 연결을 끊는 일이다.

`0.33.0`에서 `API`를 `IFC`로 옮긴 프로젝트는 `rdl doc migrate --apply`를 다시 실행하면 태스크 링크가 함께 정리된다. 이미 이관을 마친 프로젝트에도 안전하다 — 옮길 것이 없으면 아무것도 바꾸지 않는다.


## [0.34.0] - 2026-08-19

검증 문서가 명세만 갖고 실행 결과는 태스크가 나른다. 저장은 어느 태스크의 일인지를 사람에게 덜 묻는다. Board가 위계를 세운다.

### 검증 문서와 실행 결과를 나눈다

TST 문서 하나가 명세와 실행 결과를 함께 들고 있었다. 재실행하면 정본을 고쳐야 했고 그래서 지난 회차가 남지 않았다. 이 저장소의 검증 문서 17건에는 "미충족"으로 남은 체크박스가 13개 있었는데, 근거를 다시 재 보니 전부 통과하고 있었다 — 아무도 갱신하지 않아 문서가 낡은 상태를 주장하고 있었다.

- 문서는 무엇이 참이어야 하는지만 적는다. 통과 기준의 체크박스를 거부하고(`RDL-SCENARIO-004`), 시나리오는 문서 밖에서 가리킬 수 있도록 첫 열이 ID인 표를 요구한다(`RDL-SCENARIO-001`~`003`).
- 이번 회차에 실제로 통과했는지는 태스크가 답한다. 태스크에 종류와 판정을 두었다 — 진행 상태와 판정은 다른 축이다. 실패한 테스트도 수행은 끝났으므로 `done`이고 판정이 `fail`이며, 한 필드에 섞으면 "실패를 확인한 것"과 "아직 안 돌린 것"이 같은 값이 되어 테스트만 모아 성공 여부를 묻는 일이 처음부터 불가능해진다.
- 차수는 1부터 시작하는 프로젝트 전역 정수이고 (TST × 차수) 하나에 태스크 하나다. 문자열이면 2차와 R2가 다른 회차가 되어 모아 셀 수 없다.

### 결박을 파생한다

`0.33`이 저장에 결박을 걸었지만 그 답을 사람에게 물었다. 그런데 대부분의 경우 기계가 이미 안다 — 런이 돌고 있으면 그 런이 무엇을 하는 중인지 원장에 있고, 작업 묶음은 브랜치로 이미 묶여 있다. 아는 것을 묻는 통제는 확인이 아니라 요금이고, 요금을 무는 통제는 우회된다.

- 파생 순서는 **런 → 작업 묶음 → 단일 진행 태스크**다. 런이 작업 묶음을 이기는 이유는 런이 더 좁기 때문이다 — 한 브랜치에 여러 런이 있을 수 있어도 한 런은 하나의 일이다. 인수 지정은 둘 다 이긴다.
- `rdl run start --task <TASK-ID>`로 런이 자기가 어느 태스크의 일인지 밝힌다. 런 시작에 고정되고 도중에 바뀌지 않는다 — 바뀔 수 있으면 그 런이 만든 커밋들이 서로 다른 태스크를 가리킨다. 선택 필드이므로 이 필드가 없는 런 이벤트를 읽는 구버전은 멈추지 않는다.
- 무엇이 골랐는지 결과에 남는다(`taskSource`). 모르면 틀렸을 때 어디를 고쳐야 하는지도 모른다.
- 파생된 태스크도 결박의 조건을 그대로 지난다. 끝난 태스크는 파생으로도 묶이지 않는다 — 파생이 사람이 지정할 수 없는 것을 묶어 주면 파생 자체가 우회가 된다.

### Board가 위계를 세운다

- 조치 필요를 태스크로 묶고 등급으로 거른다. 문제 하나가 한 줄이면 같은 태스크가 서너 번 반복되고, 목록이 잘리면 뒤의 태스크는 아예 보이지 않았다. 사람은 문제가 아니라 태스크 단위로 일한다. 시험 워크스페이스에서 27줄이 9줄이 된다.
- 등급을 알약 대신 점 하나로 말한다. 알약은 배경과 여백으로 자리를 차지해 태그가 서넛 붙으면 행이 알약 띠가 되고 정작 제목이 묻힌다.
- 테스트 태스크의 차수와 판정을 화면이 읽는다. 묶음은 `status`가 갖고 판정은 행에 따로 붙는다.
- 브랜드가 왼쪽 패널의 머리로 올라가고 헤더는 그 오른쪽만 덮는다. 헤더 오른쪽에 성격이 다른 셋(알림·입력·조작)이 같은 상자 모양으로 줄지어 있던 것을 갈랐다.
- 아이콘을 글리프에서 SVG로 바꿨다. Windows 기본 폰트에서 컬러 이모지로 폴백되거나 획이 얇아 버튼 안에서 미아처럼 보이던 것들이다.
- 동기화 상태를 그림으로 말한다. 브랜드 마크도 글자 타일 대신 로고 그림이다.

### 알려진 한계

- `implementationReadiness`는 여전히 저장된다. `task.links`에서 결정되는 값이므로 없애려 했으나, 없애면 그 필드가 없는 옛 태스크에도 원자 계약 검사가 새로 걸려 진단이 넓어진다. 조용히 넓히지 않고 남긴다 — 이관 경로를 함께 내는 별도 변경으로 다룬다.


## [0.33.4] - 2026-08-19

프로세스 잠금이 상호 배제를 보장하지 못하는 경우가 있었다. `0.33.3`이 고친 것의 반대편이다.

### 고친 것

- **죽은 잠금을 치우다가 살아 있는 잠금을 지울 수 있었다.** 잠금이 죽었다고 읽은 뒤 옮기기 전에 다른 프로세스가 그것을 치우고 자기 잠금을 만들면, 치우는 쪽이 옮기는 것은 살아 있는 잠금이다. 그대로 지우면 두 프로세스가 모두 잠금을 쥔다. 이제 옮긴 뒤에 그것이 죽었다고 판단한 그 잠금이 맞는지 확인하고, 아니면 되돌린다.
- **만든 것과 쥔 것을 구분한다.** 잠금 파일을 배타적으로 만들었더라도 다른 프로세스의 회수가 그것을 지울 수 있으므로, 만든 뒤에 자기 토큰이 그대로인지 확인하고 아니면 다시 겨룬다.

두 상태를 경쟁이 아니라 `isAlive` 훅으로 끼어들어 결정적으로 재현하는 회귀 시험을 붙였고, 방어를 껐을 때 그 시험이 실패하는 것을 확인했다.


## [0.34.0] - 2026-08-19 (상세 기록)

위 `0.34.0` 항목과 같은 판이며 그 상세다. 병합이 두 벌로 남겼고, 요약은 위쪽이 정본이다.

테스트 문서가 화면을 근거로 서되, 화면 없는 기능이 검증에서 빠지지 않는다.

### Added

- **화면이 있는 기능은 그 화면을 검증 문서가 참조해야 합니다(`RDL-IMPL-018`).** 웹 프로젝트에서 테스트는 사용자가 밟는 경로를 근거로 서야 하는데, 지금까지 TST는 REQ만 보고도 통과했다. 다만 요구를 TST의 필수 관계 교체로 구현하지 않았다 — 그렇게 하면 인증·배치·웹훅·공개 API처럼 화면 없는 기능이 검증 대상에서 통째로 빠지고, 커버리지 분모가 조용히 줄어든다. 그래서 조건부다: TST의 필수 관계는 REQ로 남고, 그 TST가 검증하는 기능에 SCR 정본이 **실제로 있을 때만** 그 SCR 참조를 요구한다. 판정은 이미 계산하던 기능 추적성 인덱스를 읽으므로 새 저장소가 없다. 일반 검사에서 경고, `--implementation` 준비도 게이트에서 오류다. 근거는 `ADR-013`.
- **`ui` trait가 SCR을 `required`로 올립니다(이전에는 `recommended`).** 화면이 있다고 선언한 제품에서 화면 정본은 권고가 아니라 전제이고, SCR이 없으면 `RDL-IMPL-018`이 아무것도 잡지 못한다. 승격은 trait를 선언한 프로젝트에만 적용되며, 정책을 직접 지정한 프로필은 영향을 받지 않는다. 문서 유형 사이의 관계 규칙은 전역이고 trait는 프로젝트별 선택이므로, "이 제품에 화면이 있다"는 관계가 아니라 trait로 표현한다.

### Changed

- **스킬이 검증 문서의 각 칸을 어느 문서의 어느 절에서 끌어올지 지시합니다.** `입력과 데이터`는 SCR의 `바인딩`, `실행 절차`는 `사용자 흐름`, `기대 결과`는 `상태`·`전이`와 REQ의 `수용 기준`에서 온다. `RDL-IMPL-006`은 자리표시자를 거부하지만 모호한 문장은 거부하지 못한다 — "유효한 값을 입력하면 성공한다"는 검사를 통과하면서 테스트로는 쓸모가 없다. 그 간극은 검사가 아니라 지시문이 메운다.

- **검증 문서의 시나리오가 문서 밖에서 참조 가능한 ID를 갖습니다(`RDL-SCENARIO-001~003`).** `시나리오` 섹션은 첫 열이 ID인 표여야 하고, ID는 문서 안에서 유일하며 공백 없는 32자 이내 토큰이어야 한다. 실행 기록이 `TST-017#S-03`으로 결과를 붙이려면 가리킬 자리가 있어야 하는데, 목록으로 적은 시나리오에는 그 자리가 없다. ID 표기를 한 가지로 통일하지는 않는다 — 문서 식별자가 앞에 붙으므로 필요한 성질은 통일성이 아니라 안정성과 유일성이다.
- **`통과 기준`에 체크박스를 두지 않습니다(`RDL-SCENARIO-004`).** 통과 기준은 무엇이 참이어야 하는지를 적고, 이번 실행에서 실제로 통과했는지는 실행 기록이 답한다. 체크박스를 정본에 두면 재실행할 때마다 문서를 고쳐야 하고 "지난주에는 실패했다"가 남지 않는다.

- **태스크가 종류를 갖고, 테스트 태스크는 진행 상태와 별개인 판정을 갖습니다.** `rdl task add --kind test`로 만들고 `rdl task set --result <pass|fail|blocked|skipped>`로 판정을 남긴다. 판정은 상태와 다른 축이다 — 실패한 테스트도 수행은 끝난 것이라 상태는 `done`이고 판정이 `fail`이다. 한 필드에 섞으면 "실패를 확인한 테스트"와 "아직 돌리지 않은 테스트"가 같은 값이 되어, 테스트만 모아 성공 여부를 묻는 일이 처음부터 불가능해진다. 테스트 태스크는 검증한 TST 문서를 연결해야 하고(`RDL-TASK-029`), 완료할 때 판정이 필요하다(`RDL-TASK-028`). 반려는 수행하지 않기로 한 것이므로 요구하지 않는다.
- **`rdl task list --kind test`가 판정별 집계를 함께 냅니다.** 응답의 `results`는 상태 집계와 같은 규칙을 따른다 — 선택된 프로젝트 범위에서 세고 필터 이전 값을 쓴다. 아직 판정이 없는 테스트는 `pending`, 반려한 테스트는 `cancelled`로 나눠 센다. 돌릴 예정인 것과 돌리지 않기로 한 것을 한 통에 담으면 대기 건수가 실제보다 부풀기 때문이다.

- **테스트 태스크가 실행 차수를 갖습니다.** 1부터 시작하는 프로젝트 전역 정수이며 `(TST 문서 × 차수)` 하나에 태스크 하나다(`RDL-TASK-029~032`). 정수로 두는 이유는 표기가 갈리지 않게 하려는 것이다 — 문자열이면 `2차`, `2차 `, `R2`가 서로 다른 회차가 되어 모아 세는 일이 불가능해진다. 정렬과 비교가 자명해 회귀 판단이 그냥 되고, "지금 몇 차인가"도 따로 저장하지 않고 태스크들의 최댓값이 답한다. 반려한 태스크는 자리를 비우므로, 잘못 만든 것을 반려하고 같은 차수에서 다시 만들 수 있다.
- **`rdl test rounds`가 차수 목록과 차수별 범위를 냅니다.** `--round <n>`을 주면 그 차수의 태스크와 판정 집계, 그리고 **태스크가 없는 TST 목록**을 함께 낸다. 차수 대상 목록은 어디에도 저장하지 않는다 — 태스크를 만든 것이 곧 그 차수의 범위이고, 빠진 것은 문서 전체와 대조해 계산한다. 목록을 파일로 두면 정본이 둘이 되어, 목록에는 다섯인데 태스크는 셋인 상태를 아무도 알아채지 못한다.
- **`rdl task list --round <n>`으로 차수를 좁힐 수 있습니다.** 차수는 `--kind`와 달리 표시 필터가 아니라 `--project`와 같은 범위다. 2차를 물었는데 전체 차수의 집계가 돌아오면 답이 질문과 어긋나므로, 집계도 그 차수 안에서 센다.

### Fixed

- **구현 준비도 게이트가 테스트 실행 태스크까지 막았습니다.** TST를 연결했다는 이유로 `atomic-v1` 표식이 붙어, 시나리오를 밟기만 하는 실행 태스크가 REQ 연결을 요구받고 완료되지 못했다. 준비도는 "이 태스크가 구현을 시작해도 되는가"를 묻는 게이트이고 실행 태스크는 구현하지 않으므로 대상에서 제외한다.
- **검증 문서 17개가 유지되지 않는 상태를 주장하고 있었습니다.** 미충족(`- [ ]`)으로 남아 있던 13개 항목의 근거를 실제로 측정해 보니 전부 참이었다 — `collaboration-store`, `board`, `board-workspace`, `board-ui`, `doctor`, `skill-install`, `release`, `packages` 시험과 `version:check`, `test:install`이 모두 통과한다. 체크박스가 상태를 주장했지만 아무도 갱신하지 않아 낡은 것이다. 측정으로 대체한 뒤 체크박스 37개를 제거했고, 시나리오가 목록이던 `TST-016`에 ID를 가진 표를 세웠다.

### 알려진 한계

- 집계와 원자성의 축은 화면이 아니라 기능 ID로 유지됩니다. 화면은 기능 여럿을 조합하므로, 테스트 문서를 화면 단위로 조직하면 기능 하나를 바꿀 때 문서 여럿이 따라 바뀝니다.
- 검사는 빈칸을 거부할 뿐 모호한 문장을 거부하지 못합니다. 구체성은 작성 지침에 기대고 있으며 강제되지 않습니다.
- 테스트 태스크는 어느 TST를 수행했는지는 밝히지만 어느 시나리오가 통과했는지는 밝히지 않습니다. ①에서 붙인 시나리오 ID를 아직 아무도 참조하지 않습니다.
- 한 차수 안의 재실행 이력은 남지 않습니다. 같은 태스크의 판정이 바뀔 뿐이라 "2차에서 처음엔 실패했다"는 Git 이력에만 있고 조회로는 나오지 않습니다. 발견한 결함을 별도 태스크로 만들어 연결하는 것이 그 자리를 대신합니다.
- 차수를 한 번에 여는 명령은 없습니다. 태스크를 하나씩 만듭니다.
- 기능 여럿을 검증하는 문서에서 시나리오가 어느 기능에 속하는지는 선언하지 않습니다. 기능별 통과율이 필요해지면 그때 열을 추가합니다.
- 테스트 실행 결과를 어디에 둘지는 정해지지 않았습니다. TST 문서는 여전히 명세와 실행 증거를 함께 들고 있습니다.

## [0.33.3] - 2026-08-19

macOS에서 두 번째 `rdl watch`가 "이미 돌고 있다" 대신 파싱 오류로 실패할 수 있었다.

### 고친 것

- **프로세스 잠금이 만들어지는 중의 파일을 깨진 것으로 읽었다.** 잠금 파일은 여는 것과 쓰는 것이 한 동작이 아니어서 만들어진 직후 잠깐 비어 있다. 그 틈에 다른 프로세스가 읽으면 JSON이 아니고, 그것이 치명적 오류로 올라가 경쟁이 결함으로 보고됐다. 이제 몇 번 다시 보고, 그래도 못 읽으면 유효한 점유가 아니라고 판단해 회수한다 — 잠금을 쥔 프로세스는 자기 잠금을 읽을 수 없게 두지 않는다.
- **읽을 수 없는 잠금 파일이 도구를 영영 막을 수 있었다.** 회수 경로가 죽은 pid에만 걸려 있어서, 내용이 깨진 파일 하나가 남으면 사람이 손으로 지우는 것 말고는 나갈 길이 없었다. 회수 조건에 "읽을 수 없음"을 더했다.

이 두 상태를 경쟁이 아니라 결정적으로 재현하는 회귀 시험을 붙였다.


## [0.33.2] - 2026-08-19

macOS CI가 잡은 두 가지를 고쳤다. 배포된 `0.33.1`은 동작하지만, 저작 어댑터가 매번 필요 없는 Git 호출을 셋 더 하고 있었다.

### 고친 것

- **저작이 fan-out이 아닐 때도 형제 경로를 물었다.** 병렬 저작을 위해 넓힌 것은 "무엇이 남의 변경인가"의 기준인데, 형제가 없는 경우에도 그 질문을 해서 저작 한 번마다 Git 호출이 셋 늘었다. 이제 형제가 있을 때만 묻는다.
- **탈출 검사가 자기 대상까지 감시에서 빼고 있었다.** 형제 경로만 빼야 하는데 자기 대상도 함께 빠져, 격리를 벗어나 본 트리의 그 파일을 직접 고친 저작이 이 검사를 지나갈 수 있었다. 되돌림 경로의 해시 대조가 그 경우를 따로 잡고 있었으므로 실제로 새는 구멍은 아니었지만, 검사가 자기 몫을 하지 않는 상태였다.
- **어댑터 트리 종료 시험이 경쟁이었다.** 취소를 고정 지연으로 보내 느린 기계에서는 자식이 손자를 띄우기 전에 취소가 도착했고, 그러면 시험은 트리 종료에 대해 아무것도 말하지 못한 채 파일 없음으로 죽었다. 이제 손자가 뜬 것을 보고 취소하며, 그래도 못 보면 그 사실을 단언으로 남긴다.


## [0.33.1] - 2026-08-19

`0.33.0`은 배포되지 않았다. 태그를 건 커밋이 CI의 릴리스 검사에서 멈췄고, npm에는 아무것도 올라가지 않았다. 내용은 `0.33.0` 항목과 같다.

### 고친 것

- **Board 자산 시험이 커밋되지 않은 픽스처 파일에 기대고 있었다.** 시험이 읽는 `sample.png`와 `secret.txt`가 픽스처 디렉터리의 `.gitignore`(`/projects/`)에 걸려 저장소에 들어가지 않았다. 로컬에는 있고 CI에는 없으므로, 로컬 게이트가 초록인 채로 배포가 CI에서 깨졌다. 이제 시험이 그 파일을 직접 만들고 끝나면 지운다 — 픽스처가 조용히 빠지는 덫이 남지 않는다.


## [0.33.0] - 2026-08-19

문서 유형 하나가 이름을 바꾸고, 저장이 어느 태스크의 일인지 묻기 시작하며, 하네스가 여러 판정자를 동시에 부른다.

### 파기 — 올리기 전에 읽을 것

- **문서 유형 `API`가 `IFC`(인터페이스)로 바뀌었고 `STD`(표준)가 더해졌다.** `API`는 HTTP만 연상시켜 CLI·이벤트·파일 형식·어댑터 계약을 담기에 좁았다. 이름을 바꾸면서 이관 경로를 세 곳에 냈다 — 계약은 옛 이름을 새 이름으로 읽고, `board.json`은 옛 표시 키 `api`를 `interface`로 읽으며, `rdl doc migrate --apply`가 `API-###` 문서의 ID·kind·태그와 그 문서를 가리키는 모든 참조를 함께 옮긴다. 옮기기 전까지 `rdl check`는 `RDL-DOC-010`으로 알리되 막지 않는다. 자동 경로가 있는데 막으면 버전을 올린 것만으로 프로젝트가 멈춘다.
- **같은 Workspace를 이 판보다 낮은 버전과 함께 쓸 수 없다.** 계약에 `IFC`와 `STD`가 들어가면 그 유형을 모르는 판이 `project.md`를 거부한다. 이건 오진이 아니라 선언된 파기다. 혼합 버전 시험도 종료코드가 아니라 "선언된 파기 말고 다른 오류가 없는지"를 보도록 바꿨다 — 종료코드로 묶어 두면 파기가 넓어져도 시험이 같은 얼굴을 한다.
- **`projects/workspace/board.json`이 기본값을 담지 않는다.** 새로 만드는 파일은 사람이 덮어쓴 것만 갖는다. 기본값을 파일에 복사해 두면 유형을 하나 더할 때마다 공유 파일이 바뀌고, 같은 저장소를 보는 구버전이 모르는 키에서 멈춘다. 이미 있는 파일은 그대로 읽힌다.
- **태스크 식별자가 26자에서 8자로 줄었다.** 저장하는 작업이 어느 태스크의 일인지 밝히도록 요구하려면 사람이 옮겨 적을 수 있어야 하고, 칠 수 없는 식별자를 요구하는 통제는 우회된다. `rdl task identity --apply`가 옛 식별자를 옮기면서 옛 이름을 `previousIds`에 남긴다 — 이미 만들어진 커밋의 trailer와 원장의 이벤트에는 옛 식별자가 그대로 남으므로, 매핑이 없으면 과거 감사 기록과 지금의 태스크를 다시 이을 수 없다.

### 저장이 어느 태스크의 일인지 묻는다

태스크 등록은 기계적으로 동작했다. 아무도 등록하지 않았을 뿐이다. 한 판에서 열두 차례의 외부 검증을 반영하고 여러 판을 배포하는 동안 태스크는 한 건도 만들어지지 않았다. 규칙을 문서에 적는 것으로는 지켜지지 않는다 — 지켜진 것은 실제로 막는 자리에 건 것뿐이다.

- 강제는 저장에 건다. 공유에 걸면 늦다. 작업이 로컬에 쌓인 뒤에는 어느 커밋이 무슨 일이었는지가 추측이 된다.
- 강제 수준의 축이 둘이다(`enforcement`, `taskEnforcement`). 문서 계약과 태스크는 성숙도가 다르므로 하나의 값에 묶으면 낮은 쪽에 맞춰지고 이미 지켜지던 쪽이 함께 약해진다. 태스크 축은 경고에서 시작하므로 **이 변경만으로 기존 프로젝트의 동작은 달라지지 않는다.**
- 진행 중인 태스크가 하나면 그것으로 추론하고 무엇을 골랐는지 알린다. 둘 이상이면 추론하지 않는다 — 틀린 결박은 결박이 없는 것보다 나쁘다. 끝난 태스크에도 묶이지 않는다.
- 우회(`--no-task <사유>`)는 거부 수준에서만 있고 사유를 설정에 저장하지 않는다. 저장되는 우회는 한 번 켜면 계속 통한다.
- 어느 태스크의 일이었는지는 커밋의 `Rundol-Task` trailer가 답한다. 원장에 적으면 커밋과 그 사실이 따로 움직이고, 어긋났을 때 어느 쪽이 사실인지 알 수 없다.
- 이 강제는 Rundol의 저장을 지나는 작업에만 걸린다. Git을 직접 쓴 커밋에는 걸 방법이 없고, 그 한계는 없앨 수 없지만 감출 이유도 없다 — `rdl check` 요약이 최근 커밋의 결박·우회·미결박·끊긴 결박을 세어 보여 준다. 막지 않는 통제가 값을 가지려면 얼마나 쓰였는지 셀 수 있어야 하고, 세려면 진단 수십 개 사이가 아니라 요약에 있어야 한다.

### 검증이 여러 판정자에게서 나온다

`requireAdapterDiversity`는 켤 수는 있지만 만족될 수 없는 설정이었다. 허용 어댑터가 언제나 하나로 좁혀졌으므로 다양성은 영원히 1이었고, 켠 프로젝트는 결과가 계속 "사람 판단 필요"로 나왔다. 켠 사람은 그것이 판정인지 자기 설정 실수인지 알 수 없었다.

- 검증 스텝이 어댑터를 목록으로 고정한다(`verify.adapters`). 렌즈마다 다른 판정자를 붙일 수도 있다(`verify.lensAdapters`) — 재현 렌즈는 스크립트 실행기가, 누락 렌즈는 읽는 판정자가 답한다.
- slot에서 어댑터로 가는 배분은 순수 함수다. 무작위로 고르면 재개했을 때 같은 slot이 다른 판정자의 결과를 재사용하게 된다.
- **만족될 수 없는 다양성 선언은 절차 로드와 검증 시작에서 거부한다.** 런타임 실패로 두면 오설정과 정상 판정이 같은 얼굴을 한다.
- 렌즈가 자기 접근 방식(`static`·`dynamic`)과 필수 여부를 선언한다. 선택 렌즈가 실행되지 못하면 검증은 완료하되 그 렌즈를 미실행으로 보고한다 — 실행되지 못한 것과 실행되어 할 말이 없던 것(`abstain`)이 같은 값이 되면 "확인 못 함"이 "이상 없음"이 된다.
- 판정에 접근 방식이, 발견에 재현 방법(`reproduce`)이 함께 남는다. 요약만 있으면 읽는 사람은 그 판정을 믿거나 처음부터 다시 조사하는 두 길밖에 없다.
- `reproduction-v1` 렌즈를 더했다. 동적이고 선택이며, 답하는 것은 모델이 아니라 스크립트 실행기다 — 판정자가 모델일 필요는 없다.

### 판정과 저작이 동시에 돈다

- 검증 판정 호출이 상한 있는 동시 실행으로 바뀌었다(`verify.maxConcurrency`, 기본 2). 판정 호출은 서로의 결과를 읽지 않는데도 완전히 순차였고, 그래서 판정자를 늘리면 시간이 선형으로 늘었다 — 독립성을 얻는 대가로 속도를 잃으면 느려진 통제는 꺼진다.
- 형제 문서를 동시에 저작한다(`fanOut`, `adapter.authorConcurrency`, 기본 2). 격리는 이미 있었고 병목은 복귀였다 — 저작 A가 결과를 옮겨 놓으면 저작 B가 그 파일을 남의 변경으로 보고 시작을 거부했다. 청결 검사와 샌드박스 탈출 검사의 기준을 fan-out 대상 전체로 넓혔다. 병렬이 저작의 경계를 넓히지는 않는다 — 형제 목록에 없는 파일이 떠 있으면 여전히 거부한다.
- 실행되지 못한 대상과 실행되었으나 변경이 없던 대상을 다른 값으로 남긴다. 변경 여부는 선언이 아니라 대상 파일의 해시로 판정한다 — 어댑터가 스스로 신고하게 두면 아무것도 하지 않고 "변경 없음"이라 말하는 것이 완료로 가는 가장 싼 길이 된다.
- 실패를 모아서 받는다. 하나가 실패해도 나머지를 중단하지 않으며, 성공한 것은 다음 시도에서 재사용된다. 그래도 마지막에는 던진다 — 미완을 통과로 접으면 "확인 못 함"과 "이상 없음"이 같은 값이 된다.

### 빨라진 것

명령마다 저장소 위치를 여러 번 묻던 것을 프로세스 안에서 한 번만 묻는다. 캐시는 위치 질의에만 걸리고 `worktree`·`init`·`clone`이 무효화하며 `RUNDOL_NO_GIT_CACHE=1`로 끌 수 있다. 통합 배포판은 자식 프로세스 대신 CLI를 직접 들인다.

### 알려진 한계

- 결박 파생이 아직 없다. 런이 도는 중에도 `--task`를 사람에게 묻는다. run 이벤트에 태스크가 없어 이을 자리가 없기 때문이며, 다음 판이 그것을 나른다.
- `implementationReadiness`는 `task.links`에서 결정되는데 저장된다. 링크가 바뀌어도 갱신 경로가 없어 조용히 어긋날 수 있다. 다음 판에서 없애고 읽을 때 계산한다.
- 여러 원장의 상태를 사용자 행동 단위로 묶어 보여 주는 계층이 없다. 사람 대기 하나가 절차·런·결정·위임·승인·공유 판정에 흩어져 있다. 이 판단의 근거와 다음 순서는 ADR로 남겼다.

## [0.32.0] - 2026-08-19

보드가 그림과 본문을 다루기 시작하고, 승인 권한이 프로젝트에 묶인다.

### Changed

- **`rdl sync --share-unverified`에 `--approved-by <human-client-id>`가 필요합니다(파괴적 변경).** 사유 문자열은 누구나 적을 수 있으므로, 사유만 요구하는 우회는 에이전트가 자기 판단으로 사람 게이트를 넘는 길이었다. **기존 호출이 깨집니다** — `rdl sync --share-unverified "<사유>"`를 쓰던 곳에 `--approved-by <human Client ID>`를 추가하세요. 해당 Client는 `--type human`으로 등록되어 있고 이 프로젝트의 활성 멤버가 소유해야 합니다(`RDL-SYNC-031`).
- **문서 목록의 `목록 / 파일 트리` 전환 버튼을 내렸습니다.** `app.js`에 핸들러가 없어 눌러도 아무 일이 일어나지 않던 자리입니다. 그 자리에 검색 범위(문서명·본문 전체)와 정렬(ID순·최근 수정순) 컨트롤을 두었습니다. 파일 트리는 계층 뷰와 함께 다시 만듭니다.

### Added

- **보드가 문서의 그림을 표시합니다.** 지금까지 정적 경로가 UI 자산과 라이브러리뿐이어서 캡처를 넣어도 404였고, 화면설계 문서를 텍스트로만 쓸 수밖에 없었다. 프로젝트 자산 경로(`/api/projects/:key/assets/*`)를 열되 범위를 좁게 잡았다 — 프로젝트 루트 안 containment를 심링크를 따라간 뒤 다시 확인하고, 그림 확장자만, 16MB 상한, SVG를 위한 sandbox CSP. 문서 안의 상대 경로는 그 문서가 놓인 폴더를 기준으로 푼다.
- **문서 검색이 본문을 포함합니다.** 범위를 문서명과 본문 전체로 나눌 수 있고, 본문 검색에서는 맞은 자리를 발췌해 보여 줍니다.
- **문서 목록에 수정일과 최근 수정순 정렬이 생겼습니다.** 값은 이미 스냅숏에 있던 것으로, 화면에 드러내기만 했습니다.

### Fixed

- **프로젝트 밖 human Client의 승인이 통과했습니다.** 승인자 검사가 유형과 상태만 보고 소유 멤버를 보지 않아, 옆 프로젝트의 검토자가 git 병합으로 들어와 이 프로젝트를 승인할 수 있었다. 공유 판정과 감사 양쪽에서 프로젝트 활성 멤버까지 대조한다.
- **이미 공유된 런이 다시 차단됐습니다.** `0.31.2`에서 넣은 "완료 커밋 == 지금 HEAD" 검사를 `synced` 런에도 걸어, 새 런이 HEAD를 옮기면 이미 나간 런이 영원히 차단 대상이 되었다. 그 커밋은 되돌릴 수 없으므로 막는 의미가 없다.
- **커밋을 만드는 스텝이 커밋 없이 성공을 보고할 수 있었습니다.** 그런 런은 검증이 결박될 곳이 없는 채로 통과해 "무엇을 검증했는가"에 답하지 못한다.
- **문서 목록 첫 행과 마지막 행의 hover 배경이 둥근 모서리 밖으로 나왔습니다.** 같은 구조인 `.settings-panel`에는 이미 `overflow: hidden`이 있었고 한쪽만 빠져 있었다.

### 알려진 한계

- 교차 OS CI가 아직 초록이 아닙니다. Windows adapter 자손 종료, macOS·Ubuntu의 watch lock 경쟁 시험이 남아 있습니다.
- 문서 계층 구조 보기, 긴 문서의 목차, 기능 단위 묶음(REQ·SCR·TST)은 아직 없습니다.


## [0.31.3] - 2026-08-18

적대적 시험을 게이트에 넣고, 그 시험이 찾아낸 것을 고친다.

### Added

- **적대적 시험 묶음(`test/adversarial.test.js`).** 정상 경로가 도는지가 아니라 원장에 손댄 클라이언트가 무엇을 얻는지 묻는다. 비활성 승인자, 미등록 승인자, 승인 이벤트 사후 변조, 변조된 원장 위의 공유 — 넷 다 쓰기 경로를 지나오지 않는 공격이며, git 병합으로 들어온 남의 이벤트가 정확히 그 처지다. 격리 탈출 탐지도 종단 시험에 들어갔다.
- **`RDL-RUN-031`** — 사람 승인의 clientId가 활성 `human` Client인지 검사가 실제로 생겼다. 0.31.1까지 이 코드는 주석에만 있었다.
- **`RDL-RUN-033`** — 런 이벤트의 canonical 봉투 검증. 이미 기록된 이벤트의 값만 바꿔치기하는 사후 변조를 감사 시점에 잡는다. 접기는 런을 읽을 때만 알아차렸고, 감사에서는 읽기 전에 물어야 한다.

### Fixed

- **승인의 신원을 스텝 상태보다 먼저 묻는다.** 자격 없는 주체에게 "지금은 그 스텝이 아니다"라고 답하면 자격 없음이 상태 문제로 읽히고, 언제 다시 오면 되는지를 알려 주는 셈이 된다.


## [0.31.2] - 2026-08-18

`0.31.1`의 적대 경계 다섯 곳을 닫는다. **`0.31.1`은 사용하지 마세요** — 에이전트가 사람 승인을 위조해 공유할 수 있습니다.

### Fixed

- **위조된 사람 승인이 공유까지 통과했다.** 접기는 `basis: human-approval` 이벤트를 형태만 보고 승인으로 받아들였고, 승인자가 실제로 `human` 유형 활성 Client인지 대조하는 곳이 어디에도 없었다. 에이전트가 이벤트를 원장에 직접 써 넣는 것만으로 사람 승인이 됐다. 코드 주석은 이 대조를 `RDL-RUN-031`이 한다고 적어 두었지만 그 검사는 구현된 적이 없다 — 주석에 적은 통제를 구현했다고 착각한 것이고, 이 판에서 두 번째 같은 실수였다. 이제 공유를 정하는 쪽이 승인자 신원을 registry에 다시 묻는다.
- **완료 뒤에 쌓은 커밋이 승인 없이 push됐다.** 런의 통과는 그 런의 커밋에 대한 것인데, 그 커밋이 조상이라는 이유로 위에 쌓인 것까지 함께 나갔다. 이제 `completed_local`의 커밋과 지금 push되는 tip이 같을 때만 그 런이 공유를 통과시킨다.
- **저작이 격리 worktree를 빠져나가도 성공으로 보고됐다.** 어댑터는 넘겨받은 절대 경로에서 본 저장소를 역산해 직접 고칠 수 있는데, 실행 전후 스냅숏이 sandbox 기준이라 그 변경이 관측되지 않았다. 본 저장소의 HEAD와 변경 경로도 함께 스냅숏해서, 탈출한 저작은 `ADAPTER_ESCAPED_SANDBOX`로 실패한다. **여전히 OS 샌드박스가 아니다** — 쓰기 자체를 막지는 못하고, 탐지하고 거부한다.
- **`--share-unverified`를 에이전트가 쓸 수 있었다.** 사유 문자열은 누구나 적을 수 있으므로, 사유만 요구하는 우회는 에이전트가 자기 판단으로 사람 게이트를 넘는 길이었다. 이제 `--approved-by <human-client-id>`가 함께 필요하다(`RDL-SYNC-031`).
- **v2로 시작한 런에 v3 이벤트가 이어 붙었다.** 옛 판독기는 멈추지 않고 v3 이벤트만 버린 채 낡은 진행 상태를 사실로 내놓았다 — 판이 달라도 다른 답을 할 뿐 멈추지 않았다. 런의 스키마는 `run.started`가 정하고 그 뒤로 바뀌지 않는다(`RDL-RUN-032`). legacy(v1) 런을 canonical로 올리는 기존 전환은 그대로다.


## [0.31.1] - 2026-08-18

`0.31.0` tag는 release check를 통과하지 못해 어떤 산출물도 배포되지 않았다. 내용은 `0.31.0` 항목과 같고, 아래 둘을 고쳤다.

### Fixed

- **`package-lock.json`을 재생성하면서 monorepo 설치 구조가 깨졌다.** 버전만 바꿔야 할 lockfile을 `npm install --package-lock-only`로 다시 만들어, workspace 패키지가 로컬 link 대신 registry 의존으로 기록됐다(`packages/*/node_modules/@rundol/*`). 아직 배포되지 않은 버전을 요구하므로 `npm ci`가 모든 플랫폼에서 실패했다. 이 저장소는 `.npmrc`의 `workspaces=false`·`install-links=true`로 루트 설치가 workspace 패키지를 건드리지 않게 되어 있고, lockfile은 그 구조를 유지한 채 버전 문자열만 바뀌어야 한다.
- **타볼 호환성 픽스처가 원장 스키마 v2를 단정하고 있었다.** v3 전환에 맞춰 갱신했다. 이 스위트는 `npm test`가 아니라 `npm run test:install`에 있어서 전체 게이트가 덮지 않았다 — 게이트가 초록인 것과 릴리스 조건을 만족하는 것은 다른 말이다.


## [0.31.0] - 2026-08-18

이 판은 하네스가 문서를 저작·검사·저장·검증하고 사람 앞에서 멈추는 종단 경로를 처음으로 완주시킨다. 그 과정에서 드러난 안전 결함들을 함께 닫았고, run 원장 스키마를 v3으로 올렸다.

**용도 경계**: 지금 이 판은 단일 사용자 도그푸딩을 위한 것이다. 다중 클라이언트 팀 공유와 무인 자동 실행은 아직 권하지 않는다. 이유는 아래 "증명하지 않은 것"에 적었다.

### Changed

- **run 원장 스키마가 v3이다(파괴적 변경).** `run.step.commit`, `run.forced.basis`/`commit`, `run.resumed.grantedAttempts`/`reason`이 더해졌다. 0.30.2 이하 클라이언트는 v3 런을 읽지 못한다고 말하고 멈춘다(`RDL-RUN-021`) — 새 필드를 조용히 흘려 버리고 "승인 없는 런", "커밋을 모르는 저장"으로 읽는 일은 없다. **같은 Workspace를 쓰는 모든 클라이언트를 함께 올리세요.** 반대로 v2 이벤트에 새 필드를 실으면 알 수 없는 필드로 거부된다.
- **사람 게이트를 지나는 길이 `rdl run approve`로 분리됐다.** `rdl run step --force`는 운영자의 우회로 남고, 승인은 별도 행위가 된다. 원장은 둘을 `basis`로 구분한다.
- **승인은 `human` 유형 Client만 할 수 있다.** `rdl client register <id> --type human`으로 등록한다. human 자격으로는 `drive`를 비롯한 실행 명령이 거부된다. 이것은 인증이 아니라 귀속이다 — 아래 "증명하지 않은 것" 참조.
- **`rdl save`가 `--run`과 `--expect-head`를 받는다.** 런이 시킨 저장은 그 런의 대상 문서 하나에만 닿고, 기대한 커밋 위에서만 쌓는다.
- **`rdl sync`가 사람 게이트를 통과하지 못한 런의 커밋을 공유하지 않는다**(`RDL-SYNC-030`). 사람의 판단이라면 `--share-unverified <사유>`로 지나갈 수 있고, 이 우회는 저장되지 않으므로 매번 다시 말해야 한다.

### Fixed

- **저작 어댑터가 프로젝트 전체를 고칠 수 있었다.** 대상 밖 변경을 발견해 실행을 거부해도 그 변경은 이미 작업 트리에 있었고, 어댑터가 스스로 만든 커밋은 이미 브랜치에 있었다 — 발견은 되돌림이 아니다. 저작은 이제 일회용 detached worktree에서 돌고, 끝나면 대상 파일 하나만 본 트리로 옮긴다. OS 샌드박스는 아니다.
- **검증이 판정 대상을 주변의 현재 HEAD에서 주워 왔다.** 저장이 만든 커밋이라는 보장이 없어, 그 사이 다른 프로세스가 커밋하면 저작 결과가 아닌 것을 저작 결과로 판정했다. 이제 원장이 기록한 산출 커밋에 결박된다(`step-output-commit`).
- **승인 뒤 HEAD를 옮겨 다른 커밋을 완료·공유할 수 있었다.** 승인은 자기가 본 커밋을 기록하고, 완료와 공유가 그 커밋을 다시 대조한다.
- **재개가 시도 예산을 지웠다.** halt와 resume을 되풀이하는 것만으로 `maxAttempts`가 무한이었다. 예산을 다시 여는 것은 이제 `--grant-attempts <스텝> --reason`이라는 별도 결정이고, 사유 없는 개방은 접기가 무시한다.
- **여기서 풀 수 없는 리비전을 지목한 판정이 증거로 읽혔다.** 다른 클라이언트의 판정은 git 병합으로 들어오는데 그 클라이언트의 커밋이 함께 오지 않을 수 있다. `RDL-VERDICT-015`가 그 판정은 이 저장소에서 증거가 아니라고 말하고, 커밋이 도착하면 스스로 사라진다.
- **대상 문서를 만들지 않는 절차가 대상 없이 시작됐다.** `requiresArtifact`를 선언한 절차는 시작할 때 `--artifact-id`를 묻는다.

### 증명하지 않은 것

정직하게 남긴다. 아래는 이 판에서 확인되지 않았다.

- **렌즈의 판별력을 실제 어댑터로 측정한 적이 없다.** 종단 시험은 스텁 어댑터를 쓴다. 증명한 것은 배선과 방어이지, 나쁜 문서가 실제로 반려되는가가 아니다.
- **세 OS 프로세스 종료 행렬이 검증되지 않았다.** Windows에서 어댑터 자동 실행은 여전히 기본 차단이다.
- **staging ref를 채택하지 않았다.** 검증 전 초안이 로컬 이력에 커밋되고, 반박된 중간 커밋은 최종 커밋의 조상으로 남는다. 공유는 사람 승인 전까지 막히지만, 승인 뒤에는 그 초안들도 함께 올라간다.
- **`human` 유형은 인증이 아니라 귀속이다.** Rundol은 키보드 앞에 누가 있는지 모른다. 이 유형이 보장하는 것은 "이 자격으로는 실행 명령이 거부된다"뿐이며, 자격 자체를 사람이 쥐고 있는지는 등록한 사람들의 약속에 달려 있다.
- **결정·승인 원장이 어떤 명령의 게이트로도 걸려 있지 않다.**


## [0.30.2] - 2026-08-18

### Fixed

- **지나간 질문을 대상으로 한 잘못된 대체가 진단 없이 사라졌다.** 살아 있는 질문의 답변만 먼저 골라 낸 뒤 대체 관계를 검증해서, 대체된 질문에 달린 답변이 없는 이벤트·자기 자신·분기·순환을 가리켜도 아무 진단이 남지 않았다. 상태를 바꾸지 못하는 것과 잘못이 기록되지 않는 것은 다르고, 감사 원장에서는 후자가 더 나쁘다. 대체 관계를 질문(요청 투영)별로 검증한 뒤 현재 질문의 답변만 상태 계산에 쓴다.

## [0.30.1] - 2026-08-18

### Fixed

- **시험이 위험 실행 모드를 뒤따르는 스위트로 흘려보내고 있었다.** Windows 어댑터 자동 실행 옵트인을 켜기만 하고 되돌리지 않아, 같은 프로세스에서 뒤이어 도는 스위트가 전부 위험 모드로 돌았다. "기본 차단 모드에서 검증된다"는 말이 사실이 아니었다는 뜻이다. 켠 스위트가 끝날 때 되돌린다 — 켜는 것과 켠 채로 두는 것은 다른 일이다.
- **얕은 복제를 무결성 확인 완료로 판정했다.** 과거 이력이 없는 저장소에서 append-only 검사가 "위반 없음"을 돌려주어, 잘라낸 구간의 변조가 전부 무사통과했다. 얕은 복제와 잘린 이력(grafted), 공유 worktree 부재를 각각 확인 불가 사유로 낸다(`RDL-LEDGER-002`).
- **지나간 질문을 겨냥한 사칭 답변이 진단 없이 사라졌다.** 살아 있는 질문의 답변만 먼저 걸러 내는 순서 때문에, 대체된 질문을 대상으로 사후에 끼워 넣은 사칭이 상태를 바꾸지는 못하되 감사 기록에서도 지워졌다. 상태를 못 바꾸는 것과 있었다는 사실이 지워지는 것은 다르다. 유효성과 권한을 먼저 보고 살아 있는지는 마지막에 본다.

## [0.30.0] - 2026-08-18

### Changed

- **`rdl init`이 문서 목표를 결정하지 않으면 프로젝트를 만들지 않는다(파괴적 변경).** 지금까지는 `--profile`을 주지 않으면 조용히 `service`로 만들어졌다. 이제 `--profile <이름>`으로 선언하거나 `--defaults`로 권고안 수용을 명시해야 하며, 대화형 터미널에서는 인터뷰가 뜬다. **기존 자동화 스크립트가 깨집니다** — `rdl init <키> --name <이름>` 호출에 `--profile` 또는 `--defaults`를 추가하세요. 무엇을 결정해야 하는지는 `rdl init --questions --json`이 알려준다.

### Fixed

- **인가 판정에 쓰는 시각을 다이제스트가 덮는다.** 기록 시각을 canonical 밖(`occurredAt`)에 두고 권한 판정에 쓰면, 같은 이벤트의 시각만 취소 전으로 바꿔도 다이제스트가 그대로여서 취소된 위임이 다시 통한다. 판정에 쓰는 값과 다이제스트가 덮는 값이 어긋나면 안 되므로, 인가용 시각을 canonical 안의 별도 필드로 옮겼다. 이제 그 값을 고치면 다이제스트가 달라지고, 원본이 이미 공유된 뒤라면 같은 eventId에 다른 다이제스트가 생겨 상충으로 잡혀 상태를 바꾸지 못한다. **남은 한계**: 원장에 단조 시퀀스가 없으므로 처음 기록할 때부터 거짓 시각을 적는 것은 여전히 막지 못한다 — 그 해소는 작성자 시퀀스 번호 채택 결정에 달려 있고 `src/authority.js` 머리말에 적었다.
- **위임의 수임 Client도 접기가 검증한다.** 검사(`RDL-DLG-012`)는 등록되지 않은 수임 Client를 지목하는데 접기는 그대로 활성으로 세어, 검사와 실제 권한 소비가 다른 답을 냈다. 검사를 통과했다는 말이 권한이 안전하다는 뜻이 되려면 둘이 같은 답을 내야 한다.
- **결정 요청 충돌을 CLI에서 해소할 수 있다.** 접기에는 요청 대체를 넣어 놓고 쓰기 경로를 열지 않아, 명령은 성공하고 결정은 계속 열려 있었다. `rdl decision request --supersedes`로 상충 요청을 대체한다. `decision answer --delegation`도 값이 전달되지 않고 있었다.
- **`rdl doc create --grouped --reason`이 실제로 동작한다.** 명령이 플래그를 파싱만 하고 `createDocument()`에 넘기지 않아, 기능 ID가 둘 이상인 TST·MOD·API 문서를 CLI로 만들 수 없었다. 거부 경로에만 시험이 있어 이 누락이 통과했다 — REQ는 선언이 있어도 다기능을 거부하므로 거부 시험은 버그와 무관하게 통과한다. 허용 경로 시험을 붙였다. **이 버그는 사용자에게 잘못된 조언을 만들었다**: 같은 기능 ID가 두 문서에 있다는 진단(`RDL-IMPL-016`)에 대해 정당한 해소책이 합침 선언인데 그 경로가 막혀 있어, 문서를 쪼개라는 권고가 나갔다.
- **Windows에서 프로세스 트리 종료 실패를 성공으로 돌려주지 않는다.** `taskkill /T /F`가 접근 거부로 실패해도 성공한 Promise를 반환해, 취소가 끝났다고 보고되면서 자손 프로세스는 살아 있었다. 실패하면 남은 수단을 쓰고, 그래도 살아 있으면 그 사실을 남긴다.
- **인가를 현재 상태가 아니라 기록 시점의 사실로 판정한다.** 처음에는 "지금 유효한가"로 판정했고 그것이 정반대 두 가지를 동시에 깨뜨렸다 — 만료·취소된 위임이 영원히 통했고(상태를 아예 보지 않았다), 반대로 Client를 비활성화하면 그 장치가 과거에 정당하게 남긴 승인·결정·위임이 통째로 탈락했다. 이제 **현재 권한과 역사적 귀속을 나눈다**: 쓰기 경로는 "지금 남길 수 있는가"를 보고, 접기는 "그 기록이 남던 시점에 남길 수 있었는가"만 본다. 취소는 미래에만 작용한다. 그러려면 행위가 언제 기록됐는지가 필요한데 세 원장 모두 남기지 않고 있었으므로 append 시점에 기록 시각을 찍는다(canonical 밖이라 기존 다이제스트는 그대로다). **이 방식이 보장하지 않는 것도 적어 두었다**: 원장에 단조 시퀀스가 없어 수임 Client가 자기 샤드의 기록 시각을 앞당겨 적으면 취소 뒤의 행위를 취소 전인 것처럼 위장할 수 있다. 없애려면 인과 순서가 필요하고 그것은 event-store가 정할 일이다.
- **멤버 경계를 세 원장이 같이 본다.** 인가 검증이 멤버 목록을 받아 놓고 검사하지 않아, 결정 답변만 별도로 멤버를 확인하고 승인·위임은 Client 소유자만 봤다. 한 원장만 검사하면 다른 원장으로 우회된다. 결정 **요청**도 인가한다 — 답변만 지키면 반쪽이고, 위조 요청 하나로 아무 결정이나 열린 상태로 만들어 진행을 막을 수 있었다.
- **요청 충돌도 해소할 수 있다.** 같은 결정 키에 서로 다른 요청 내용이 들어오면(`RDL-DEC-019`) 되돌릴 방법이 없어 그 결정이 영구 교착이 됐다. 대체 규칙을 한 곳으로 모아 요청과 답변이 같은 규칙을 쓰게 했다 — 한쪽에만 탈출구를 주면 다른 쪽 충돌로 막히고, 한쪽에만 제약을 주면 다른 쪽이 새 공격면이 된다.
- **위조된 승인·위임이 채택되던 경로를 닫았다.** 인가 검증이 원장마다 흩어져 있었고 그중 둘(승인·위임)에는 아예 없었으며, 있는 하나마저 `rdl check`가 인가 없이 불러 껐다. 그래서 CLI를 지나지 않고 Git 병합으로 들어온 승인이 문서를 `approved`로, 위조된 위임이 `active`로 만들 수 있었고 그 판정이 문서 분석까지 흘러갔다. 확인을 한 모듈에 모으고 **인가 컨텍스트를 접기의 필수 조건**으로 만들었다 — 없으면 접기가 거부한다. 안전한 경로가 opt-in이면 언젠가 꺼진다. 행위자 자리에는 위임이 서지 못한다: 위임은 "누가 책임지는가"를 옮길 뿐 "누가 실제로 눌렀는가"를 바꾸지 못하며, 책임자가 행위자와 다를 때만 위임이 그 차이를 정당화하고 부여자·수임 Client·종류가 모두 맞아야 한다.
- **결정 충돌 해소와 위임 답변이 실제로 동작한다.** `--supersedes`와 위임 식별자가 이벤트에 실리지 않아 두 기능 모두 표면만 있고 경로가 없었다 — 명령은 성공하고 결정은 계속 열려 있었다. 값을 실어 저장하고 접기가 검증한다. 잘못된 대체(자기 자신·없는 대상·분기)는 진단만 남기고 답변을 살려 두어 결정을 `answered`로 만들 수 있었는데, 이제 그 답변을 무효화한다. 승인 위임이 조회하던 `doc-approve` 종류가 결정 카탈로그에 없어 부여도 조회도 성립하지 않던 것을 등록했다.

### Added

- **`rdl doc analyze`가 무엇을 봐야 하는지 좁혀 준다.** 분석은 읽을 것을 줄이는 것이지 읽기를 대신하는 것이 아니다 — AI 요약이 원문 읽기를 대체하면 이번에 세운 승인 모델의 취지가 무너진다. 그래서 담긴 것은 전부 기계가 판정할 수 있는 구조적 사실뿐이다. 역참조(이 문서를 무엇이 가리키나), 신뢰 상태, **고아**(아무도 가리키지 않고 태스크도 기능 ID 연결도 없는 문서), **기록 없는 정본**(승인도 연결된 태스크도 없는 문서)을 낸다. 마지막 것은 "이 문서에 대해 남은 기록이 아무것도 없다"는 뜻이며, **"지금 이 내용이 왜 이렇게 됐는가"에는 답하지 않는다** — 연결된 태스크는 과거 어느 시점의 것이라도 세므로 그 뒤 리비전이 몇 번 바뀌었든 기록이 있는 것으로 나온다. 리비전별 조인은 아직 없다. 역참조는 저장하지 않고 계산한다 — 저장하면 문서를 고칠 때마다 어긋난다. 고아 판정은 표시 링크만 보지 않는다. 검증 문서는 요구를 참조하지만 화살표가 되돌아오지 않으므로 표시 링크만 보면 모든 TST가 영구히 고아가 되고, 뿌리인 프로젝트 문서도 마찬가지다 — 문서 종류 하나를 통째로 오탐하면 그 신호는 죽는다. 기능 ID를 공유하는 추적성 관계도 연결로 세고 뿌리 문서는 묻지 않는다. 판단이 필요한 품질 평가는 여기가 아니라 `rdl verify`의 렌즈가 맡는다. 이 저장소에 걸어 보니 설명 없는 정본이 34건이다.
- **조회 인덱스는 내부 계층으로만 들어갔다 — 공개 명령이 아니다.** 정본에서 파생한 삭제 가능한 캐시를 만들었지만 `rdl index` 명령과 `--index` 플래그는 0.30에 넣지 않는다. 두 가지 이유가 겹쳤다. 첫째, 이 저장소에서 정본 조회 전체가 4ms인데 인덱스를 써도 되는지 확인하는 데만 259ms가 든다(`git rev-parse` 131ms + `status` 147ms) — 아끼는 것보다 확인하는 것이 비싸면 캐시는 가속이 아니라 감속이다. 둘째, "조회가 성공하면서 틀린 답을 내는" 경로가 고칠 때마다 하나씩 더 나왔다: 상태 목록만 보는 지문, 형태를 확인하지 않는 읽기, 필터를 따르지 않는 집계, C-quoted 경로 미복호화, 없는 프로젝트에서의 빈 성공. **틀릴 여지가 있고 이득이 없는 기능은 공개하지 않는 편이 낫다.** 계층과 시험은 남긴다 — 등가성·지문·스키마 검증이 계속 돌아 다시 열 때 정확성이 지켜진 상태에서 시작한다. 다시 열기 전 조건은 REQ-041에 적었다.
- **`rdl run drive`가 검증 스텝을 실제로 몬다.** 지금까지 verify 커서는 실행을 거부하고 사람이 따로 `rdl verify`를 돌려야 했다. 이제 drive가 절차에 pin된 렌즈·지시·정족수로 검증 커널을 호출하고 그 판정만 원장에 남긴다 — 통과는 커서를 전진시키고, 반박은 절차가 정한 `onFail` 경로로 되돌리며, 정족수 미달은 재시도로 풀리지 않으므로 `verification-required` 정지가 되어 사람을 기다린다. 검증 대상이 될 산출물이 없으면 실행하지 않고 멈춘다. onFail이 없는 검증 스텝은 반박됐을 때 돌아갈 곳이 없으므로 preflight가 거부한다.
- **문서에 고유 식별자가 생겼다.** 모든 문서가 생성 시 조율 없는 짧은 식별자(`uid`, 8자)를 받는다. 파일 이름과 번호는 그대로라 옵시디언의 위키 링크·그래프·백링크가 전부 살아 있다. 식별자 없는 기존 문서는 `RDL-DOC-014` 경고로 드러나고 `rdl doc identity --apply`가 부여하며, 중복은 `RDL-DOC-016`으로 두 문서를 모두 지목한다. 소급 부여는 문서 내용을 바꿔 리비전이 변하므로 기존 승인이 낡는다 — `--apply` 없이는 무엇이 바뀔지만 보여준다. **조인은 아직 번호가 담당한다.** 태스크 링크·related 위키 링크·추적성·승인 대상이 모두 번호로 조인하므로 문서 번호 채번의 원격 예약은 그대로 유지된다. 조인을 식별자로 옮기고 조율을 제거하는 것은 조회 인덱스와 함께 다음 마이너에서 한다 — 조인이 남은 채로 예약만 없애면 두 클라이언트가 같은 번호를 만들어 한쪽이 조용히 덮인다.
- **문서의 신뢰 상태를 기록하고 조회할 수 있다.** AI가 만든 문서는 승인 기록이 없으므로 미승인으로 나타나고, 사람이 `rdl doc approve`로 그 리비전을 승인하면 승인 상태가 된다. **이 상태를 저장·검사·동기화의 게이트로 강제하는 것은 아직 하지 않는다** — 게이트 연결은 다음 마이너다. 지금은 상태를 정확히 기록하고 보여주는 데까지다. 승인은 내용 리비전에 결박되므로 한 글자만 고쳐도 낡음으로 바뀌고, 나중에 "그건 다른 버전이었다"가 통하지 않는다. 신뢰 상태는 저장하지 않고 파생한다 — 승인 결과를 frontmatter에 쓰면 그 쓰기가 리비전을 바꿔 자기 승인을 무효화하고, 파생이라야 AI가 `state: active`로 적어도 소용이 없다. `rdl doc status`가 승인·낡음·미승인을 일람하고, `rdl doc history`가 승인 기록과 커밋을 함께 보여주며, `rdl doc diff --since-approval`이 승인 이후 변경분만 보여준다 — 재승인이 싸야 엄격한 무효화가 유지된다. 승인 근거(직접 읽음·verdict·검사·위임)는 필수이고 사유 문장은 선택이다. 강제된 사유는 빈 문장으로 채워질 뿐, 나중에 "AI 검토가 놓쳤나 사람이 건너뛰었나"를 구분해 주지 못한다.
- **작업 묶음이 브랜치 참조에서 파생된다.** 여러 기능을 한 브랜치에 묶어 작업하고 하나의 병합 요청으로 함께 안착시키는 흐름을 지원한다. `rdl task set --external-ref branch=<이름>`으로 참조를 붙이면 같은 참조를 가진 태스크들이 곧 묶음이 되고, `rdl workset list`가 그 묶음과 소속 태스크·병합 요청·상태를 계산해 돌려준다. 묶음은 저장하는 개체가 아니라 파생이라 새 정본 파일도 새 검증 규칙도 늘지 않는다. `--external-ref pr=<주소>`로 병합 요청을 붙이면 계약에만 있고 도달할 수 없던 `review` 전이(`RDL-TASK-020`)가 CLI로 열린다. 예전 문자열 형태의 외부 참조는 마이그레이션 없이 종류 없는 참조로 함께 읽힌다.
- **결정 위임이 권한으로 다뤄진다.** `rdl delegation grant`가 부여자·수임 Client·결정 종류·만료·사유를 공유 원장에 남기고, `rdl delegation list`로 지금 무엇이 누구에게 위임돼 있는지 보이며 `rdl delegation revoke`로 즉시 거둘 수 있다. 만료는 선택이 아니라 성질이라 기본 7일·상한 30일이며 갱신하지 않으면 효력을 잃는다. 배포·병합·강제 우회·위임 부여 자체는 부여 시점에 거부된다 — 위임을 위임할 수 있으면 경계는 이름만 남는다. 위임이 걸린 결정은 묻지 않고 진행하되 요청과 답변이 함께 기록되고, 답변자는 부여자이며 사유에 근거가 된 위임이 남는다.
- **초기 설정에서 생략이 예외가 된다.** 문서 목표를 정하지 않은 프로젝트 생성은 조용히 기본 프로필로 만들어지는 대신 거부되고, `--profile`로 선언하거나 `--defaults`로 권고안 수용을 명시해야 한다. 어느 쪽을 택했는지는 결과에 `profileSource`로 남는다. 대화형 터미널에서는 `--guided` 없이도 인터뷰가 기본이며, 대화형이 아닌 곳에서는 `rdl init --questions --json`이 결정해야 할 항목을 목적과 설명이 붙은 질문 목록으로 돌려주어 사람의 답을 플래그로 받을 수 있다. 원격과 기본 브랜치는 판단이 아니라 정보이므로 읽어서 기록하되, 원격이 있는데 기본 브랜치를 추론할 수 없으면 조용한 낙하 대신 멈춘다 — 기능 브랜치를 기본으로 삼으면 push 경계가 엉뚱한 브랜치를 보호한다.
- **사람 결정이 대화가 아니라 기록이 된다.** 되돌릴 수 없거나, 이 머신을 벗어나거나, 다른 주체의 권한을 덮거나, 요청 범위를 벗어나는 지점에서 `rdl decision request`가 질문·닫힌 선택지·권고안·가역성을 담은 요청을 공유 원장에 남긴다. **`push`·`publish`·`merge` 같은 실제 명령이 이 요청을 자동으로 만들고 멈추는 배선은 아직 없다** — 지금은 결정을 기록·조회·위임하는 계층까지이고, 명령 게이트 연결은 다음 마이너다. `rdl decision list`로 어느 클라이언트에서나 읽고 `rdl decision answer`로 답하면 결정자와 사유가 함께 기록된다. 같은 종류·범위·대상의 결정은 하나로 취급되어 다시 묻지 않으며, 답이 없을 때의 기본은 언제나 정지다. 결정 종류는 `rdl decision kinds`가 카탈로그로 노출하고, 배포·병합·강제 우회·위임 부여는 위임이 성립하지 않는 종류로 고정된다.

- **스킬이 발견 표면과 태스크 수명주기를 흐름에 담는다.** 작업 시작은 `rdl context --json`이고, 모르는 명령은 소스가 아니라 `rdl help --json`이며, 정본과 코드 변경은 태스크를 잡고(`doing`) 증거를 수용조건에 반영한 뒤 검사·동기화가 끝나야 `done`이다. 태스크 없는 변경은 조용한 지름길이 아니라 소리 내어 말하는 예외다. 사람에게 반드시 물어야 하는 결정도 명문화했다 — 되돌릴 수 없거나, 밖으로 나가거나, 다른 주체의 권한을 덮거나, 요청 범위를 벗어나는 것. 검사가 판정할 수 있는 것과 안전한 기본값이 있는 것은 묻지 않는다.
- **에이전트 발견 표면.** AI 클라이언트가 명령과 상태를 소스 탐색으로 재발견하던 비용을 없앤다. `rdl task list`는 상태·우선순위·수용조건 요약과 함께 태스크를 결정적 순서로 나열하고(`--project` 없으면 Workspace 전체), `rdl help --json`은 사람용 usage 정본에서 파생한 명령·플래그 카탈로그를 반환하며(사본이 아니라 파생이라 낡지 않는다), `rdl context --json`은 Workspace·브랜치·진단·열린 태스크와 상태에서 계산한 다음 행동을 한 번에 반환한다. 인덱스도 상시 서버도 쓰지 않는 cold 경로이며, 여기서 고정한 출력 계약을 이후 조회 인덱스가 같은 형태로 가속한다.

## [0.29.0] - 2026-08-17

### Added

- **하네스 커널 확장 — 단일 델타.** Run 원장이 schema v2 canonical envelope와 causal owner token으로 강화됐다(takeover conflict/resolution, off-cursor fencing, shared-first idempotent append, request journal 복구, pinned harness settings, same-sync `run.synced` 전파). 불변 instruction/lens registry와 격리된 one-shot adapter 실행 커널, `rdl verify`, client-sharded verdict 원장, deterministic quorum fold, verification request 재개가 추가됐다. `rdl watch`가 안정된 입력 snapshot 기준으로 진단 NDJSON과 선택적 원격 tip 관찰을 제공하고, `rdl run drive`가 멱등 절차를 정본 커서부터 사람·sync 경계까지 실행한다(operation ID, retry-safety preflight, driver lease, 충돌 감지·명시적 resolution, 프로세스 트리 취소).
- 혼합 버전 호환이 실측으로 게이트된다. 매 스위트의 빠른 경로(p15-compat)가 신형 run v2·driver lease 공유 샤드를 0.28.1 check/sync에 통과시켜 오진 0을 고정하고, `release:check`의 `test:install` 단계는 0.28.1 기준 커밋을 실제 `npm pack` tarball로 설치해 배포 산출물 그대로가 신형 데이터(run v2 수명주기·takeover·driver lease·verdict)를 오진 없이 지나가는지 확인한다. CI checkout은 기준 커밋 접근을 위해 전체 이력을 받는다.

### Changed

- **릴리스 기재 정직화.** 이 릴리스는 `0.28.1` 위의 단일 델타다. 개발 중 `[0.29.0]`~`[0.32.0]` 네 항목으로 나뉘어 기재됐으나 어느 것도 독립 릴리스로 존재하지 않았으므로(태그·배포 부재), [릴리스 정책](docs/RELEASES.md)에 따라 하나의 항목으로 통합하고 버전을 0.29.0으로 되돌린다. 교차 검증(재프로브 15건)과 후속 리뷰에서 확정된 커널 결함의 수정도 배포 전 같은 델타 안에서 이뤄졌으므로 아래 Fixed에 함께 기록한다.

### Fixed

- **fold가 이벤트 집합의 함수가 됐다.** 같은 이벤트 집합이 열거 순서에 따라 여덟 가지 fold 결과를 내던 것을 고쳤다: 순서의 정본을 병합 배열에서 각 작성자 샤드의 append 순서로 옮기고, epoch 소속을 `(ownerToken, 소유자 clientId)` 결박으로 정하며, takeover cutoff를 이전 소유자 자기 시퀀스의 위치로 해석한다. 작성자 부분열을 보존하는 교차 3,000종에서 fold 결과는 하나다.
- 타 클라이언트의 소유자 토큰 재사용이 무진단 수용되던 것을 `RDL-RUN-023`으로 진단하고 stale 처리한다. epoch에 들어오는 외래 이벤트는 sync 실행자의 `run.synced`·sync 사유 `run.halted`와 인가된 비소유자 `run.operation_resolved`뿐이며, 외래 전이는 위치가 아니라 우선순위로 적용되어 사전순 병합에서 `run.synced`가 소실되지 않는다. sync 전이 작성자도 공유를 먼저 reconcile한 union에서 소유권을 도출한다.
- 외래 sync 전이의 효력을 epoch과 커밋에 결박했다. 구 epoch 커밋의 늦은 `run.synced`가 신 epoch의 완료와 결합해 런을 synced로 만들지 못한다(`RDL-RUN-026`). sync 전이의 신원 인가는 검사 계층이 맡는다 — clientId가 활성 agent/service가 아니면 `RDL-RUN-005`.
- strict 검증이 런이 아니라 이벤트의 스키마를 따른다 — legacy 런에 유입된 v2 이벤트도 커서·종류 검증을 받는다. legacy 전용 런도 소유권·dedup·진단이 같은 단일 fold 경로를 지난다.
- 유효하지 않은 takeover 시도만 있는 런은 ACTIVE로 조용히 남는 대신 CONFLICT로 fail-closed한다(유효 takeover 도착 시 자연 해소). 토큰 없는 v2 `run.halted`는 쓰기가 거부하고 유입분은 `RDL-RUN-024`로 진단한다. 충돌 상태에서도 커서·완료 진행은 보존된다.
- 크래시 재시도가 준비된 canonical 바이트를 그대로 재사용한다 — attempt 재계산으로 digest가 갈려 멱등성이 깨지던 것을 driver-lease의 decode-재사용 패턴으로 통일했다. 같은 요청 ID에 다른 payload나 commandDigest가 오면 저널 재생이 과거 결과를 돌려주는 대신 거부한다.
- 읽기 경로의 dedup을 런 단위로 격리했다. 다른 런 샤드의 손상이 이 런의 읽기를 오염시키지 못하고, 같은 런의 충돌은 예외가 아니라 `RDL-RUN-017/018` 진단으로 fold에 흐른다. digest 정의는 정규화 하나로 남아, 원시 digest로 쓰인 v2 레코드가 legacy로 낙인되지 않는다.
- driver lease가 실행 판단에 연결됐다. leaseId가 `(operationId, clientId, ownerToken, rootRequestId)`의 함수가 되어 파티션의 두 실행자와 크래시 후 재기동이 서로 다른 유효 사슬로 노출되고, acquire 직후 read-back이 자기 사슬 invalid를 lease-lost 정지로 전이시킨다. 파티션의 다른 유효 사슬은 `leaseContention`으로 tick 결과에 노출된다 — 상호 배제 약속이 아니라 가시성이며, 실행 안전은 operationId 멱등성이 보증한다.
- 무효 takeover만 있는 fail-closed CONFLICT에 탈출구가 생겼다. 충돌이 부모 epoch의 head를 노출하고, `--force --reason` 인수가 그 head를 가리키는 정상 유효 takeover를 기록해 자기 치유한다 — 이전에는 그 takeover를 쓸 유일한 API가 CONFLICT에서 거부되어 런이 영구 교착이었다.
- drive 실패가 원장에도 남는다. 실패한 스텝은 `run.step`(exit 1)에 더해 `run.halted`(reason `step-failed`, 재개 가능)를 기록해, 반환값은 halted인데 fold는 running인 채 다음 drive가 실패 스텝을 재개 절차 없이 재실행하던 것을 막는다.
- operation 충돌은 completed_local/synced 뒤에서도 은폐되지 않는다 — `operationConflicts` 목록은 항상 노출되고, 완료 뒤에 남은 충돌은 `RDL-RUN-028` 경고로 표면화된다.
- drive는 verify 스텝을 명시적으로 거부한다(fail-closed). 일반 adapter로 오분류해 `run.step`을 기록하면 원장이 거부해 커서가 전진하지 않은 채 검증(LLM 호출)이 tick마다 무한 반복될 수 있었다. drive의 verify 배선(verifyArtifact·quorum·verdict 기록)은 다음 마이너의 기능 작업이다.
- 절차가 pin한 verify policy(validators·quorum·maxRefuted·maxAbstain·diversity)가 `verifyArtifact`에 병합된다 — 이전에는 pin이 사문이 되어 기본값 quorum 1로 검증됐다. perLens 없는 flat policy는 모든 lens에 적용되고, pin과 다른 호출자 policy는 거부된다.
- sync 전이 대상을 공유 run 샤드와의 union으로 열거한다. 로컬 `.rundol/runs`는 git으로 전파되지 않아, 새 clone의 sync 실행자가 다른 클라이언트의 런을 열거조차 못 해 synced/halted 전이가 누락됐다.
- 개행으로 끝난 malformed 마지막 행은 크래시 절단이 아니라 원장 파손으로 판정한다 — 읽기가 던지고 append도 파손 위로의 확장을 거부한다. 크래시 절단 관용은 개행 없는 꼬리에만 적용된다.
- watch의 `remoteIntervalSeconds`가 실제로 적용된다 — 검증만 되고 읽히지 않는 죽은 설정이라 원격 fetch가 스캔 주기(기본 5초)마다 나가던 것을 자체 주기로 제한한다.
- verifier의 작업 트리 불변 계약을 실패 경로에서도 검사한다. 파일을 바꾼 뒤 실패·timeout으로 끝난 verifier는 `ADAPTER_VERIFIER_MUTATED` 진단으로 귀속이 남는다.
- force 소유권 해소의 도달 불능 가드를 교정했다 — 부모 epoch 소유 멤버는 자기 소유의 다른 클라이언트로 자기 충돌을 승인할 수 없다.
- run 결박 verdict fold에 ownerToken이 없으면 명시적 오류이고, verdict eventId 충돌은 `RDL-VERDICT-004` 진단이다.
- legacy takeover의 fence 불가를 `RDL-RUN-027` 경고로 표면화한다.
- 커널 결정성·소유권·재생의 핵심 시나리오가 정식 회귀 테스트로 편입됐다.
- adapter 테스트 teardown이 Windows에서 간헐적으로 스위트를 실패시키던 것을 고쳤다(OS 임시 디렉터리 이동·읽기전용 해제·비치명 정리).

## [0.28.1] - 2026-08-16

### Changed

- `0.24.1` 릴리스 이력을 하네스 트랙(`0.25.0`~`0.28.0`)과 병합했다. `0.24.1`의 수정 커밋들은 이 트랙의 분기점 이전에 이미 포함되어 있었으므로 코드 변경은 없다 — 두 이력의 합류를 기록하는 릴리스다.

## [0.28.0] - 2026-08-16

### Changed

- **문서 1개가 기능 1개를 나르는 것이 기본 계약이 됐다.** 같은 도구로 만든 두 프로젝트의 결과가 갈렸다 — free-loan은 다기능 문서 0개, rundol 정본은 REQ 셋이 기능 5~6개씩. 규칙이 강제하지 않으니 작성 주체에 따라 달라진 것이다. 이제 다기능은 frontmatter 평면 키 `groupingReason`·`groupingFunctions`로 선언한 opt-in이며, 유형 정책이 선언의 효력을 정한다: REQ·SCR은 선언이 있어도 거부(분리가 유일한 해소), TST는 선언으로 허용, MOD·API는 허용하되 사유를 경고(`RDL-IMPL-017`)로 항상 표면화한다. `rdl doc create`는 `--function-id` 2개 이상에 `--grouped --reason`을 요구한다.
- 새 진단 `RDL-IMPL-013`(선언 없는 다기능)·`014`(금지 유형)·`015`(선언 형식 위반)·`016`(같은 기능이 같은 유형 문서 여럿에 흩어짐)은 **단계 도입**된다: 일반 검사에서 경고, `--implementation` 구현 준비도 게이트에서 오류. 문서 저장·동기화는 막히지 않지만, **위반 문서가 연결된 태스크의 준비도 검사는 오류가 되어 구현 착수가 게이트된다.** 해소는 문서 분해 또는(허용 유형에 한해) grouping 선언 추가다. 기존 문서의 정리가 끝나면 상시 오류 승격을 검토한다. REQ 간 기능 중복을 지키던 `RDL-IMPL-009`는 그대로 상시 오류다.

## [0.27.0] - 2026-08-16

### Added

- **`rdl run` — 절차 실행 관리.** 런은 목표·단계·게이트·결과를 `RUN-ID`로 묶는 실행 단위다. `start`가 절차 정의 전문을 pin하고, `next`가 커서 스텝의 실행 방법을 원장 fold에서 재계산해 반환한다 — 어떤 클라이언트든 이 인터페이스 하나만 물으면 된다. 일반 스텝은 `step`으로 완료를 보고하고, 게이트 스텝은 `gate`가 rdl 하위 명령을 셸 없이 직접 실행해 종료 코드로만 전진한다. `--force --reason` 우회는 forced 이벤트로 남는다. 명령을 내리는 주체는 런의 현재 소유자이며, 다른 클라이언트는 `takeover`를 거쳐야 쓸 수 있다.
- **절차 상속.** 내장 기본값 → Workspace `projects/workspace/procedures.json` → 프로젝트 `projects/<key>/procedures.json`. 오버라이드는 스텝 추가와 파라미터 조이기만 — 게이트 제거, 게이트 명령 변경, 시도 상한 확대, 사람 게이트 제거, 부모 스텝 순서 변경은 로드 시점에 거부된다. 유효한 절차 이름과 병합 결과는 한 함수가 단일 소스로 계산한다. 런이 pin한 정의는 이후 변경·삭제와 무관하게 유효하다.
- **sync 전이.** sync가 성공하면 `completed_local` 런이 `synced`로 전이한다 — 런의 완료는 저장이 아니라 병합 생존이다. sync의 어떤 실패든 진행 중이거나 로컬 완료인 런을 재개 가능한 `halted`로 전이시킨다.

### Fixed

- Windows에서 이벤트 저장 락 획득이 드물게 실패했다. 다른 프로세스의 락 해제(rmdir)와 겹친 획득(mkdir)이 `EEXIST` 대신 일시적 `EPERM`을 반환할 수 있는데 재시도 대상이 아니었다. 일시 오류를 재시도하고 대기 한도를 늘렸다.

## [0.26.0] - 2026-08-16

### Added

- **run 원장이 workspace 브랜치로 승격됐다.** 커서를 결정하는 모든 런 이벤트가 `projects/workspace/events/run/`의 클라이언트+런 샤드로 복제되고, 커밋은 체크포인트에서만 만들어진다. 다른 머신이 로컬 상세 없이 공유 이벤트만 읽어도 다음 스텝·시도 횟수·정지 사유가 소유자와 동일하게 복원된다 — 시도 횟수가 공유되지 않으면 인수가 시도 상한을 우회하기 때문에, 공유의 기준은 빈도가 아니라 커서 결정성이다. 구버전 check는 `events/`의 디렉터리를 건너뛰므로 이 경로를 보지 못하고 오진하지 않는다. 0.24.0 코드를 그대로 실행하는 혼합 버전 테스트가 이를 증명한다.
- **런 인수(takeover).** 자동 인수는 이전 소유자의 정지(halted)가 보일 때만이다. 정지 없이 죽은 런은 사람이 `--force`와 사유로 인수하며 forced로 기록된다. 벽시계 TTL은 어떤 인수 판정에도 쓰이지 않는다 — 머신 간 클록 스큐는 보정할 수 없다. 크로스 클라이언트 이벤트 순서는 시계가 아니라 takeover 이벤트의 `previousClientId` 연결로 복원한다.
- `rdl check`가 run 샤드를 검사한다: 파일명 패턴(`RDL-RUN-001`), Client 등록(`RDL-RUN-002`), 파일명과 이벤트 필드의 일치(`RDL-RUN-003`), JSONL 파싱(`RDL-RUN-004`). 그 밖의 `events/` 서브디렉터리는 미래의 이벤트 종류로 보고 진단하지 않는다.

## [0.25.0] - 2026-08-16

### Added

- **로컬 run 원장.** 목표→단계→게이트→결과를 `runId`로 묶어 프로젝트 로컬 `.rundol/runs/<runId>/events.jsonl`에 기록하는 append-only 원장이 생겼다. 진행 상태는 저장되지 않고 읽기 시점의 결정적 fold로 계산되므로, 프로세스가 어디에서 죽어도 다음 읽기가 커서·시도 횟수·정지 사유를 그대로 복원한다. 시도 상한은 정지 이벤트 없이도 fold가 강제하며, 재개는 사람 개입으로 보고 시도 예산을 되살린다. 크래시로 절단된 마지막 줄은 읽기가 무시하고 다음 기록이 같은 규칙으로 잘라낸다. 공유 브랜치에는 아무것도 쓰지 않는다 — Git에 새 파일 종류가 생기는 것은 다음 마이너의 일이다.
- `rdl action record`가 `runId` 상관 필드를 받는다. 주지 않으면 기록은 이전과 바이트 단위로 같다.

### Changed

- lease 이벤트 저장의 샤드 선택·세그먼트 롤오버·읽기 검증이 범용 이벤트 스토어(`src/event-store.js`)로 추출됐다. 파일 경로·이벤트 형태·오류 메시지는 그대로다. 같은 client의 CLI 프로세스 여럿이 동시에 임대를 기록하면 세그먼트 롤오버의 「읽고 판단하고 append」가 경합할 수 있었는데, 머신 단위 락으로 직렬화했다. clientId는 실행 주체 단위 식별자라 프로세스 경합까지 막지는 못하기 때문이다.
## [0.24.1] - 2026-08-16

### Fixed

- **`0.24.0` deleted settings people had typed.** The absorption block was treated as machine-generated defaults, but the old contract screen let you enter required components by hand and toggle authoring order, so that block held a mix of defaults and human input. One `rdl contract set` erased it. Values now survive the save: not being read and being safe to delete are different things. `rdl check` reports what is left (`RDL-PROFILE-012`, `RDL-PROFILE-013`) and `rdl contract migrate` moves the components to their new home — per-type preset sections are the same concept relocated. Authoring order has no new home, so migrate names what would be dropped and leaves the decision to you. A project already saved under `0.24.0` has lost those values; they are recoverable only from Git history. See [the migration guide](docs/MIGRATION-0.24.md).
- Switching projects discarded task edits still inside the 500ms debounce. Sending them to the wrong project was already prevented, but a change the user had just clicked vanished without a word. Pending edits are now flushed first, and anything that cannot be sent is named before the switch proceeds.
- Editing only a type's sections left "save as preset" hidden, because the comparison looked at policy alone while a preset defines both policy and sections. Adding or removing a section now re-evaluates too.
- `rdl doc create` merged preset sections into the template instead of replacing them, so a section the team had removed still appeared in the skeleton and the generated document disagreed with what `contract show` reported. The preset now decides the list and the order. Function contracts are appended after the rebuild — their heading differs by type (설계 for REQ, 검증 for TST), so matching on the text dropped the whole block for TST.
- Saving a preset and switching the contract to it touch two different files, and a failure in the second left the preset alone with the screen holding a stale revision, so retrying was refused as a conflict. The snapshot is refreshed either way and the message says what was written and what still needs doing.
- `rdl project profile` rejected team presets that `rdl contract set` accepted, so the same name worked or failed depending on the command.

## [0.24.0] - 2026-08-16

### Changed

- **호환성 파괴 — `documentProfile`이 저장하는 범위가 줄었습니다.** [마이그레이션 안내](docs/MIGRATION-0.24.md). `rules.<TYPE>.after` and the absorption half of `omissions` are no longer project state. Authoring order never differed between projects and never blocked anything, so it is now a shipped constant and `rdl contract next` returns what it always did. Absorption checked only whether a heading string existed: six empty headings satisfied it, while a document that covered the subject thoroughly under different headings failed it, and enabling the type later left the absorbed content in place with nothing pointing at it. Four diagnostics (`RDL-PROFILE-006`, `007`, `010`, `011`) went with it. A `project.md` written by this version is rejected by `0.23.0`, so upgrade a shared workspace together. Nothing needs to be run: old blocks still parse, and the next contract save drops them.
- **A recorded `notApplicable` decision and its reason survive.** They are not a machine-generated default — they are a person's judgement about why a type does not apply, and nowhere else holds them. Removing the absorption rule was not a reason to erase them.
- Document types now carry the sections their documents are expected to fill, on every type rather than only disabled ones. `rdl contract show --json` reports them per profile and `rdl doc create` scaffolds from the same list. The shipped defaults were extracted from 257 real project documents rather than guessed from templates.
- Profiles are no longer five hardcoded names. A team defines its own in `board.json` — label, policy, and per-type sections — inheriting built-in → workspace → project, the same chain the display settings already used. The contract stores only the profile name; what it means is recomputed on read.
- Stored values and the words on screen are separated. `required`, `checkpoint`, `lean`, `todo` and `high` reached the screen verbatim, and the profile picker used the display text as the stored value, so renaming a label would have broken saved contracts. Labels now come from the display settings and are editable per workspace and project. `onDemand` reads as 「선택」 rather than 「필요할 때」, which sounded like a schedule when it means permission.

### Fixed

- A team profile made `rdl check --strict` fail. `rdl contract show` resolved presets but the general check did not, so a custom name was misdiagnosed as unsupported and blocked save and sync — `contract check` reported valid while `check --strict` failed on the same project.
- Editing two tasks in quick succession could lose the first edit. Settling one task refreshed the snapshot, which reverted the other task's not-yet-sent optimistic change, and the next payload was then built from the reverted value.
- The contract editor's preset section fields and "save as preset" button were wired to nothing. Saving now goes through a display-settings API that writes only what this scope overrides, so inherited values are not frozen into the lower file.
- `rdl doctor` hardcoded a Node floor of 14 while the package required 20, so it reported a healthy environment that could not run Rundol. It reads `engines` now.
- Switching projects left scheduled task saves and the open detail panel behind: the save would fire against the new project's path, and the panel kept showing an item absent from the new list.
- The home screen's four metrics were inert `div`s. They now open the list each number came from.

## [0.23.0] - 2026-08-16

### Changed

- **호환성 파괴 — `engines.node`가 `>=14`에서 `>=20`으로 올라갔습니다.** [마이그레이션 안내](docs/MIGRATION-0.23.md). The `>=14` floor had never been true: the direct dependency `marked` requires `>=20`, CI verified only 20 and 22, and the install guide said 14. Node 14–19 installed and then failed on first run. `>=20` records the range that actually works rather than removing capability, but `npm install` now succeeds and fails in different places, so it is a compatibility break. Data, document contracts, task storage, CLI arguments and the Board API are unchanged; no migration command is needed.
- This change already shipped in `0.22.9` and `0.22.10`, which were released as PATCH with no changelog entry. That contradicts the repository's own [release policy](docs/RELEASES.md), which requires a MINOR bump and a documented migration for a compatibility break. `0.23.0` re-releases the same content under the correct classification. On Node 20 or newer the three versions behave identically.

### Fixed

- Consecutive task edits could be rejected. After a save the queue entry was cleared before the fresh snapshot arrived, so a click landing in that window started a new queue entry stamped with the pre-refresh revision and the next request was refused with HTTP 409. The entry now survives until the snapshot lands, and changes made in between are carried into it. The guard that keeps polling from replacing the snapshot mid-edit was doing two different jobs — protecting a document draft and protecting optimistic task rendering — and has been split, so the save path can take the new revision while the document draft protection stays absolute.
- A task panel stayed open after moving to another screen. Tasks and People were allowed to keep the panel as a pair, so opening a task and then going to People left the previous task's panel on screen with its selection already cleared, and the reader column stayed narrowed for a panel that no longer belonged there.
- Composite views recorded a source commit they could not prove. When `git status` itself failed the worktree was treated as clean and the current HEAD was written as the view's origin, even though checking that commit out might not reproduce the view. What cannot be proven clean is no longer claimed as clean.

## [0.22.10] - 2026-08-16

### Fixed

- Sequence diagram participant names spilled outside their boxes. Mermaid sizes each box by measuring the label in the font from its own config, but the style it injects into the SVG never applied, so the text was actually drawn in the browser's default monospace. Measured width and drawn width disagreed and long names like "Project readers" ran past the border. Both now use the same font.

## [0.22.9] - 2026-08-16

### Fixed

- Diagrams in document bodies were unreadable. Flowchart edges rendered as large black wedges: their paths carry the class `flowchart-link`, not `edgePath`, so the rule meant to stroke them never matched and mermaid's default black fill filled the inside of every curve. Sequence actor names were white text on white boxes, because the shape-fill rule matched `.actor` and `text.actor` is a label, not a shape. ER cardinality markers were black blobs, because crow's feet are drawn with strokes and every marker path was being filled. Edge colour came from the divider token, the weakest hairline tone in the palette, which left arrows too faint to trace from one node to the next.
- Wide flowcharts shrank to fit the reader column until their labels reached an effective 7px. A diagram that is fully visible but illegible shows nothing. Diagrams now stop shrinking at an 11px floor and scroll horizontally past it.
- The task Board was unusable. The page scrolled instead of the lanes, so lane headers slid out of view and there was no way to tell which column was on screen; cards were wider than their lane and spilled out both sides, because the implicit grid track sizes to max-content; card titles were clipped mid-word on one line and short titles sat centred, both inherited from the global button rule. The Board is now pinned to the viewport, each lane scrolls inside itself, the header and filters stay put, and cards share one height so columns can be compared by eye.
- Saving a document from the Board rewrote parts of the file nobody had edited: it dropped the blank line after the closing `---` and converted CRLF line endings to LF. A document saved without a single change came back as a whole-file diff, mixing real edits into noise. Saves now restore the separator and line endings the file already used.
- Saving the document contract replaced every omission disposition with catalog defaults. A type marked not-applicable — a decision the settings screen cannot express — lost its disposition and its recorded reason on the next save from that screen. Dispositions are now merged per type, and a payload that does not carry a decision leaves the current one alone.

### Changed

- A document lease is a coordination signal, not a permission to save, and `REQ-020` now says so. Enforcing it would mean that a browser dying mid-edit leaves a five-minute lease that locks everyone else out with nothing to do but wait — worse than anything the lease prevents. Loss is prevented by `baseRevision` and the branch boundary instead. Saving a document held by another client now succeeds and reports who holds it and until when, rather than being refused.

## [0.22.8] - 2026-08-15

### Fixed

- The `0.22.7` release never published. Its lockfile carried a release-version stamp on a transitive dependency, so `npm ci` refused to install and both the release and CI workflows stopped before doing anything. This release carries the same changes with a lockfile regenerated by npm.

## [0.22.7] - 2026-08-15

### Added

- Tasks can be closed as `cancelled`. Work that was decided against had nowhere to go: `done` demands satisfied acceptance criteria and a linked TST, so it could not pass, and forcing it through would record "완료" for a deliverable nobody will ever find. The two terminal states carry opposite gates — `done` requires the evidence, `cancelled` presupposes its absence and requires a reason and a decider instead, enforced as strictly as the `waiting`/blocker pair so cancelling cannot become a quiet bypass. `rdl task set --status cancelled --reason <사유> [--decided-by <MEMBER-ID>]`. A terminal task no longer blocks its dependents; treating a cancelled predecessor as unfinished left successors permanently blocked with no way to clear them.
- The Board shows what is blocked. A task waiting on a person was stored but a task whose predecessor is unfinished was not shown anywhere, so the list read as if the work could start. Both are now judged in one place and marked in the list, and a dependency view draws the order for whatever the current filters select.
- The home screen answers what to do next. It separates work that can start from work that is blocked, and lists what changed since the last visit — the last-visit time is kept per browser, since one shared timestamp would hide changes on every other device.
- Members can be registered from the CLI with `rdl member add|set|list`. `project.md` stays canonical and the Board does not edit it.

### Fixed

- Quick add on the task screen sent no acceptance criteria, which the API requires, so every quick add failed with HTTP 400. The one-line form is gone; tasks are created through the dialog that collects a completion condition properly.
- Polling replaced the snapshot while a document was being edited. Only the redraw was skipped, so the draft's base revision was silently refreshed and saving it passed the optimistic concurrency check — overwriting whatever someone else had changed in the meantime. The snapshot is now left alone while editing.
- The Board snapshot inherited the task list API's default limit of 100. The snapshot is the whole working set the screen filters locally, so beyond 100 tasks the remainder vanished at once from the list, personal queue, attention items and dependency checks, with nothing indicating the loss.
- A generated client id embedded the machine's hostname, and that id becomes shard directory and event file names committed to the repository. `MOD-002` forbids host information in the manifest body but it was leaking through file names instead. New ids hash the hostname to six characters, so a machine keeps a stable identity without publishing its name.
- Pair-consistency errors carried no status code, so the Board returned HTTP 500 for what is a caller input mistake. `blocker` had the same problem.

### Changed

- The Board's chrome is split by what it owns. The header keeps only what does not depend on the project — search, sync state, attention count, the viewing identity and settings — and the sidebar keeps navigation within the project. A single sidebar carrying all of it overflowed the screen.
- Both side panels collapse to an icon rail instead of disappearing. Hiding them moved the content by their full width on every toggle and left the reopen control to be hunted for; overlay panels are gone entirely, so behaviour no longer depends on window width.
- Task detail is one component rendered either in the side panel or as a full page, ordered title, properties, then content. The two used to be separate markup, so the same task showed different things depending on where it was opened.
- The operations screen is gone. Its sync and attention cards duplicated the header and home screen and its watch card was a placeholder; the edit leases it uniquely showed moved to Workspace settings.
- Presentation rules are read-only in the Board and name the `board.json` that owns them. Client registration and removal stay with `rdl client`; the Board only toggles the active flag.
- Polling backs off when the tab is hidden instead of recomputing the snapshot every three seconds all day.

## [0.22.6] - 2026-08-14

### Fixed

- `rdl attach` and `rdl init`'s repair path reported composite generation failures only inside an object, and the human-readable printer skips objects, so a failure was invisible unless `--json` was passed. Failures now also surface as a plain line. A project skipped because its `.gitignore` lacks the entry is a deliberate omission and is not reported as a failure.
- A composite view generated from a worktree with uncommitted changes recorded `HEAD` as its source, so checking out that commit and regenerating did not reproduce it — the reproducibility the document standard promises. The revision is now recorded only when the worktree is clean, and `rdl contract diagram` reports `dirty`.
- The generated-view directory was skipped by name at any depth, so a legitimate `docs/views/` would have silently dropped out of every document check. It is now skipped only at the project root, where the generator actually writes.
- `RDL-COMPOSE-002` checked only a transition's destination. An edge starting from a screen that does not exist passed and composed a phantom node; both ends are now checked.

### Added

- Added `RDL-SCREEN-004` for a transition that starts from another screen. The screen-owns-its-exits rule was stated in the standard but never enforced, so a document could declare a neighbour's edge and the composite graph would silently lose the completeness it claims.
- `npm run version:check` now compares `package-lock.json` against the release version. The lockfile had drifted to `0.22.3` while the manifests moved to `0.22.5`, and nothing caught it.

### Changed

- The `waiting`/`blocker` invariant moved from the Board API to the shared task layer, so `rdl task set` rejects the change with a clear message instead of writing it and having projection validation roll it back with a generic `RDL-TASK-014`.

## [0.22.5] - 2026-08-14

### Added

- Extended `diagram-v1` to `SCR`. A screen document now declares the moves that leave it in a `전이` section with a Mermaid `flowchart`, canonical in the transition table above it. `rdl check` reports `RDL-SCREEN-001` when the diagram is missing, `RDL-SCREEN-002` when a node is not a `SCR` identifier, and `RDL-SCREEN-003` when an edge returns to its own screen.
- Added `rdl contract diagram --project <key> [--write] [--json]`, which merges every `MOD` `erDiagram` into one data view and every `SCR` `flowchart` into one screen view. It reports `RDL-COMPOSE-001` when two documents claim attributes on the same entity and `RDL-COMPOSE-002` when a transition points at a screen that does not exist — defects no single document can see.
- `--write` places the composite in the Vault's Git-ignored `views/` so Obsidian readers see the same picture the Board does, and adds the ignore entry once for projects created before this release.
- `rdl check` now runs the same composition, so attempting the merge doubles as a consistency check: `RDL-COMPOSE-001` and `RDL-COMPOSE-002` surface without asking, and `RDL-COMPOSE-003` reports a generated view that no longer matches the canonical documents. The comparison reads the diagram body rather than the frontmatter, so a new commit alone is not drift and a hand-edited generated file is.
- `rdl attach` and `rdl init`'s repair path generate the views while connecting a project. Projects whose `.gitignore` lacks the entry are skipped so that connecting never modifies a tracked file; one explicit `rdl contract diagram --write` adds it and later attaches generate automatically.

### Changed

- Screen transitions and screen states now have one owner each: moving to another `SCR` is a transition, changing what the current screen displays is a state. The `SCR` template ships both sections and the boundary between them.
- Composite views are computed, never stored as canonical artifacts. The generator is deterministic — nodes and edges sort by identifier and nothing time-varying reaches the body — so the same inputs always produce the same bytes and an old view is reproduced by checking out that commit and regenerating. Generated files record their input documents and source commit, and report themselves stale once the canonical documents move past them.

### Fixed

- Moving a task to `대기` in the Board now asks who is being waited on, what releases the wait, and since when, and saves the status and the blocker as one change. Leaving `대기` clears the blocker in the same change. The API rejects a `waiting` status without a blocker, a blocker on any other status, and a waiting target that `project.md` does not register, so a transition can no longer be accepted and then reverted by `RDL-TASK-014` or `RDL-TASK-015`.
- Board primary buttons now carry their own foreground token. The light theme turns the accent into near-black, which left `+ 새 태스크` and the task dialog's save button printing black on black; the label is now white there and unchanged in the dark theme.
- The task dialog's `×` and `취소` buttons no longer submit the form. Both sat inside a `method="dialog"` form as implicit submit buttons, so dismissing an empty dialog tripped the title field's required-input validation instead of closing. Only the save button submits, and only it validates.
- Mermaid diagrams render in the Board's own palette instead of Mermaid's built-in `default` and `dark` themes, which painted lavender entities in the light theme and greys the surrounding surface could not carry. Entity fills, borders, relationship lines, edge labels and attribute rows all read from the active theme tokens and re-render on a theme switch.
- ER cardinality markers no longer keep Mermaid's hardcoded white circle fill, which showed as a solid white blob over every relationship line in the dark theme. The circle now takes the diagram's surface colour, and the diagram sits on the panel surface rather than the code-block background.
- `rdl doc create NTE` produced an unusable note. The template never closed its frontmatter, so every generated note failed `rdl check` with `RDL-DOC-001` and blocked `rdl save` for the whole project; it also carried no `owner` line, so `--owner` had nothing to fill in. The template now closes its frontmatter and ships a body.
- A screen title containing a double quote no longer breaks the composite view. The generated node label escaped nothing, so `로그인 "실패" 화면` emitted four quotes on one line and the diagram failed to parse.

### Changed (notes)

- `NTE` stays outside the artifact taxonomy, so it requires only the `rundol/` tag namespace rather than `artifact/`, `domain/`, and `feature/`. Every other metadata and identifier rule still applies, so a sub-numbered identifier such as `REQ-001-01` remains invalid; a note points at its subject through the file name and `related` instead.

### Migration

- Existing `SCR` documents report `RDL-SCREEN-001` until a `flowchart` is added to their `전이` section. This is advisory and blocks neither `rdl save` nor `rdl sync`.
- When adding the section, move any branch that stays on the same screen out of it. A login failure that renders an error is a state, not a transition, and belongs to the `상태` table.
- Never recover the transition graph from `TST` scenarios. Scenarios walk representative paths, so any edge no path crosses disappears without a trace.

## [0.22.4] - 2026-08-14

### Added

- Added the `diagram-v1` document diagram convention. `rdl contract show --json` now publishes `catalog.diagrams` so AI clients and the Board read the accepted Mermaid kinds, the canonical representation, and the selection criterion from the CLI instead of restating them.
- `MOD` documents now carry a Mermaid `erDiagram` in their `관계` section. `rdl check` reports `RDL-MODEL-001` when it is missing and `RDL-MODEL-002` when attribute comments duplicate the entity table.
- Added `conventions.<TYPE>.selection`, which decides each diagram element with one question: whether this document owns the entity's lifecycle, whether a stored field creates the relationship, and whether a cardinality is enforced at write time or at read time.
- The `MOD` template ships a conforming `erDiagram` scaffold, and the governance skill and document standard state the same selection criterion.

### Changed

- The diagram stays a derived view: entity and relationship tables remain canonical for cardinality, field constraints, and lifecycle, and only entities a document owns show attributes. Both findings are warnings, so existing model documents keep passing `rdl check --strict` until they are updated.
- State transitions stay out of relational notation. The per-function `상태와 전이` contract remains their only source of truth, so no second diagram kind was introduced.

### Migration

- Existing `MOD` documents report `RDL-MODEL-001` until an `erDiagram` is added to their `관계` section. This is advisory and blocks neither `rdl save` nor `rdl sync`.
- When adding the diagram, check whether a relationship row states a read-time constraint such as "at most one currently valid" as its stored cardinality. Fix the table first, because the diagram derives from it.

## [0.22.3] - 2026-08-14

### Added

- Expanded Rundol's canonical project specification from the recent document-contract slice to the full Workspace, synchronization, task, collaboration, Board, installation, and release lifecycle.
- Added one-function-per-file REQ and TST pairs for 11 core capabilities, plus bounded Git, Board, data-model, API, decision, runbook, and glossary documents.
- Replaced the placeholder Rundol project charter with explicit mission, goals, scope, responsibilities, risks, review cadence, and Definition of Done.

### Changed

- Rundol's own reference project now demonstrates the governance skill's preferred small-document shape without introducing a generic `DESIGN.md`, index, catalog, or persisted traceability matrix.

### Migration

- No runtime or storage migration is required from 0.22.2. Reinstall the governance skill and use `rdl contract trace` plus `rdl check --strict --implementation` when expanding an existing project's specifications.

## [0.22.2] - 2026-08-14

### Added

- Added `atomic-v1` function contracts for REQ, SCR, MOD, API, and TST documents, including type-specific standalone fields for every declared function ID.
- Added `rdl check --implementation` readiness validation and `rdl contract trace` computed traceability with no persisted index artifact.
- Added implementation-readiness task completion gates, Board contract visibility, and remote default-branch discovery through `origin/HEAD`.

### Changed

- The governance skill now forbids grouped function ranges, combined specification rows, generic `DESIGN.md`, and canonical index or traceability-matrix documents.
- Rundol's own PRD, REQ, architecture, ADR, and TST documents now describe and verify 16 independent function contracts.

### Migration

- Existing 0.21.1–0.22.1 projects should follow [Rundol 0.22 migration](docs/MIGRATION-0.22.md). Existing implementation documents remain warnings under normal strict checks and must be upgraded before enabling the implementation readiness gate.

## [0.22.1] - 2026-08-14

### Added

- Added template-derived component catalogs, non-blocking AI context guidance, and freely editable omission requirements in Board document contract settings.
- Added inherited Board presentation settings from built-in defaults through Workspace and project `board.json` files, including consistent document type and state labels.
- Added Rundol skill routing and CLI/structure diagnostics that treat generic `DESIGN.md` files as preserved migration inputs rather than canonical documents.

### Fixed

- Expanded Markdown reading width, removed the task Board's inner horizontal scroll, and improved inline-code contrast in light and dark themes.
- Unified document type and state vocabulary across navigation, recent documents, cards, lists, and document details.

### Migration

- Existing 0.22.0 projects need no contract or storage migration. Reinstall the governance skill to receive canonical design routing; optional `board.json` files can override presentation labels without changing document identity or paths.

## [0.22.0] - 2026-08-14

### Added

- Added schemaVersion 2 document planning contracts with enforcement, authoring prerequisites, omission absorption rules, and deterministic evaluation.
- Added `rdl contract show|next|check|plan|set` and shared contract visibility across bootstrap, document creation, validation, save/sync, the governance skill, and the Board.

### Migration

- Existing `0.21.1`–`0.21.3` workspaces should follow [Rundol 0.22 migration](docs/MIGRATION-0.22.md), configure the contract as advisory first, then enable checkpoint after violations are resolved.

## [0.21.3] - 2026-08-14

### Fixed

- Unix 계열 환경의 개별 CLI 전역 설치 검증이 npm의 `<prefix>/bin` 실행 파일 경로를 사용하도록 수정했습니다.

## [0.21.2] - 2026-08-14

### Added

- Bootstrap discovery와 idempotent `rdl init`/`attach` repair 경로를 통합했습니다.
- 프로젝트 `documentProfile`, guided interview, non-interactive 정책 설정을 지원합니다.
- canonical 문서 경로와 legacy 문서 migration, 통합 QA 및 사용자 문서를 정비했습니다.

## [0.21.1] - 2026-08-12

### Added

- 태스크 완료조건을 Board에서 직접 체크하고 revision 충돌을 검증해 저장한다.
- 설정 화면에 기본 색상과 고대비 흑백 모드를 추가한다.
- Markdown의 Mermaid 코드 블록을 로컬 번들로 SVG 렌더링한다.

### Fixed

- 루트를 private monorepo로 전환하고 `packages/rundol`만 배포 이름을 소유하게 해 Git URL 설치에서 발생하던 dangling package Junction을 제거했다.
- 오른쪽 Context 패널을 접은 뒤 펼치기 버튼이 잘려 복구할 수 없던 레이아웃을 수정한다.
- 데스크톱 접힘과 모바일 오버레이 상태가 동시에 남지 않도록 패널 상태를 정규화한다.
- 설정 버튼의 화면 이동과 테마 상태 표시를 회귀 테스트한다.

## [0.21.0] - 2026-08-12

### Added

- Navigation·Main·Context 3패널 Board Shell과 Documents 1급 탐색·읽기 화면
- 영역별 revision을 포함한 Workspace Snapshot과 Needs Attention 파생 상태
- base revision 및 strict validation 롤백을 적용한 Markdown 문서 편집 API와 UI
- People, Operations, Settings 분리 화면과 모바일 Navigation·Context drawer

### Changed

- Home을 단순 태스크 개수에서 최근 문서와 조치 필요 항목 중심으로 재구성했다.
- Tasks를 목록 우선으로 제공하고 Board를 선택 가능한 보기로 변경했다.

## [0.20.1] - 2026-08-12

### Added

- Workspace JSONL 이벤트와 Git CAS 재시도로 동시 문서 번호를 중복 없이 예약한다.

### Changed

- Workspace와 프로젝트 동시 fetch가 공용 `FETCH_HEAD`를 덮어쓰지 않도록 브랜치별 원격 ref를 사용한다.
- README와 상세 문서의 저장 경로, 설치, Obsidian, 명령 예제와 릴리스 정책을 일관되게 정리했다.

## [0.20.0] - 2026-08-11

### Added

- schemaVersion 6 Workspace, Client Registry와 분할 JSONL 문서 임대 이벤트
- 프로젝트 로컬 `.rundol/state`, `.rundol/logs` 실행 상태와 Workspace·Client·Lease CLI
- 멀티 프로젝트 Board Home, 프로젝트 전환, 문서·동기화·임대·Client API와 통합 snapshot
- 태스크 엔티티 revision 및 오래된 수정의 `409 Conflict` 처리

### Changed

- Workspace 공통 상태를 `projects/workspace/`, 프로젝트 상태를 `projects/<key>/`에 연결한다.
- Git safe-directory 판별을 실제 저장소 루트 기준으로 수행한다.

## [0.19.0] - 2026-08-11

### Added

- 저장소 내부 `.rundol` 없이 OS 사용자 runtime state를 사용하는 schemaVersion 5 Workspace
- 원격 `rundol/settings`와 `rundol/<project-key>`를 복원하는 `rdl attach` 및 `rdl detach`
- 프로젝트별 Obsidian Vault와 `rdl check --structure`, `rdl cleanup --apply`
- npm workspaces 기반 `core`, `protocol`, `cli`, `node`, `board`, 통합 `rundol` 패키지 경계

### Changed

- 신규 프로젝트의 Obsidian Vault 루트를 `projects/<project-key>`로 변경
- main `.gitignore` 대신 로컬 `.git/info/exclude`로 프로젝트 worktree를 숨김
- 빈 `.gitkeep` 기본 생성을 제거하고 선택 디렉터리를 필요할 때 로컬 생성

## [0.18.0] - 2026-08-11

### Added

- CLI·LLM·혼합 액션 라우팅과 실제 executor·fallback·채택률 debug 집계
- `rdl task acceptance`를 통한 완료조건 상태 변경
- 미보고 LLM 토큰 이벤트의 명시적 구분

### Fixed

- 종료 코드가 0이 아닌 검증 결과를 debug 성공으로 기록하던 문제
- PRD 입력 제목에 `제품 요구사항` 접미사를 중복 추가하던 문제

## [0.17.0] - 2026-08-11

### Added

- `rdl doctor` 구조화 설치 진단과 독립 `scripts/install-doctor.js`
- GitHub·GitLab HTTPS/SSH 설치, HTTP/1.1 fallback, PATH와 손상 설치 복구 문서
- tarball·Git URL 전역 설치와 손상 package 감지 회귀 테스트

## [0.16.0] - 2026-08-11

### Added

- `rundol/settings` 브랜치와 schemaVersion 4 Workspace
- client ID 기반 최대 500건 태스크 샤드와 단일 `tasks.json` 마이그레이션
- `rdl sync watch`, 충돌 조회·해결 CLI, 표준 문서 생성, debug token 집계
- Board 자동 갱신, 빠른 태스크 생성, drag/drop과 확장된 협업 정보 UI

### Changed

- AI 클라이언트 거버넌스 스킬이 settings와 태스크 샤드 구조를 따르도록 갱신됐다.

## [0.15.0] - 2026-08-11

### Changed

- 중복된 SPC 문서 체계를 REQ로 통합하고 CLI·문서 표준을 정리했다.
